## 1. Backend Implementation

- [x] 1.1 Add `@Schema` annotation and Javadoc to `BrazilianStates.java`
- [x] 1.2 Verify that `BrazilianStates` is used in at least one exported DTO (e.g., `AddressRequestDTO` or similar)

## 2. Verification

- [x] 2.1 Run `./mvnw compile` to ensure no syntax errors in the backend
- [x] 2.2 (Optional) Check if the generated OpenAPI JSON/YAML reflects the changes if possible in this environment
