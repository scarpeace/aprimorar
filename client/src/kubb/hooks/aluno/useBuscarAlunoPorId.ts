/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type {
  BuscarAlunoPorIdQueryResponse,
  BuscarAlunoPorIdPathParams,
  BuscarAlunoPorId404,
  BuscarAlunoPorId500,
} from "../../types/aluno/BuscarAlunoPorId.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const buscarAlunoPorIdQueryKey = (
  studentId: BuscarAlunoPorIdPathParams["studentId"] | undefined,
) =>
  [{ url: "/v1/alunos/:studentId", params: { studentId: studentId } }] as const;

export type BuscarAlunoPorIdQueryKey = ReturnType<
  typeof buscarAlunoPorIdQueryKey
>;

/**
 * @description Retorna um aluno por ID.
 * {@link /v1/alunos/:studentId}
 */
export async function buscarAlunoPorId(
  studentId: BuscarAlunoPorIdPathParams["studentId"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    BuscarAlunoPorIdQueryResponse,
    ResponseErrorConfig<BuscarAlunoPorId404 | BuscarAlunoPorId500>,
    unknown
  >({ method: "GET", url: `/v1/alunos/${studentId}`, ...requestConfig });
  return res.data;
}

export function buscarAlunoPorIdQueryOptions(
  studentId: BuscarAlunoPorIdPathParams["studentId"] | undefined,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = buscarAlunoPorIdQueryKey(studentId);
  return queryOptions<
    BuscarAlunoPorIdQueryResponse,
    ResponseErrorConfig<BuscarAlunoPorId404 | BuscarAlunoPorId500>,
    BuscarAlunoPorIdQueryResponse,
    typeof queryKey
  >({
    enabled: !!studentId,
    queryKey,
    queryFn: async ({ signal }) => {
      return buscarAlunoPorId(studentId!, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * @description Retorna um aluno por ID.
 * {@link /v1/alunos/:studentId}
 */
export function useBuscarAlunoPorId<
  TData = BuscarAlunoPorIdQueryResponse,
  TQueryData = BuscarAlunoPorIdQueryResponse,
  TQueryKey extends QueryKey = BuscarAlunoPorIdQueryKey,
>(
  studentId: BuscarAlunoPorIdPathParams["studentId"] | undefined,
  options: {
    query?: Partial<
      QueryObserverOptions<
        BuscarAlunoPorIdQueryResponse,
        ResponseErrorConfig<BuscarAlunoPorId404 | BuscarAlunoPorId500>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ?? buscarAlunoPorIdQueryKey(studentId);

  const query = useQuery(
    {
      ...buscarAlunoPorIdQueryOptions(studentId, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<
    TData,
    ResponseErrorConfig<BuscarAlunoPorId404 | BuscarAlunoPorId500>
  > & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
