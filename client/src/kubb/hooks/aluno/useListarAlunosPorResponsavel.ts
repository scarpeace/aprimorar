/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type {
  ListarAlunosPorResponsavelQueryResponse,
  ListarAlunosPorResponsavelPathParams,
  ListarAlunosPorResponsavel500,
} from "../../types/aluno/ListarAlunosPorResponsavel.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const listarAlunosPorResponsavelQueryKey = (
  parentId: ListarAlunosPorResponsavelPathParams["parentId"] | undefined,
) =>
  [
    { url: "/v1/alunos/responsavel/:parentId", params: { parentId: parentId } },
  ] as const;

export type ListarAlunosPorResponsavelQueryKey = ReturnType<
  typeof listarAlunosPorResponsavelQueryKey
>;

/**
 * @description Retorna uma lista de alunos pelo ID do responsável.
 * {@link /v1/alunos/responsavel/:parentId}
 */
export async function listarAlunosPorResponsavel(
  parentId: ListarAlunosPorResponsavelPathParams["parentId"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    ListarAlunosPorResponsavelQueryResponse,
    ResponseErrorConfig<ListarAlunosPorResponsavel500>,
    unknown
  >({
    method: "GET",
    url: `/v1/alunos/responsavel/${parentId}`,
    ...requestConfig,
  });
  return res.data;
}

export function listarAlunosPorResponsavelQueryOptions(
  parentId: ListarAlunosPorResponsavelPathParams["parentId"] | undefined,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = listarAlunosPorResponsavelQueryKey(parentId);
  return queryOptions<
    ListarAlunosPorResponsavelQueryResponse,
    ResponseErrorConfig<ListarAlunosPorResponsavel500>,
    ListarAlunosPorResponsavelQueryResponse,
    typeof queryKey
  >({
    enabled: !!parentId,
    queryKey,
    queryFn: async ({ signal }) => {
      return listarAlunosPorResponsavel(parentId!, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * @description Retorna uma lista de alunos pelo ID do responsável.
 * {@link /v1/alunos/responsavel/:parentId}
 */
export function useListarAlunosPorResponsavel<
  TData = ListarAlunosPorResponsavelQueryResponse,
  TQueryData = ListarAlunosPorResponsavelQueryResponse,
  TQueryKey extends QueryKey = ListarAlunosPorResponsavelQueryKey,
>(
  parentId: ListarAlunosPorResponsavelPathParams["parentId"] | undefined,
  options: {
    query?: Partial<
      QueryObserverOptions<
        ListarAlunosPorResponsavelQueryResponse,
        ResponseErrorConfig<ListarAlunosPorResponsavel500>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ?? listarAlunosPorResponsavelQueryKey(parentId);

  const query = useQuery(
    {
      ...listarAlunosPorResponsavelQueryOptions(parentId, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<
    TData,
    ResponseErrorConfig<ListarAlunosPorResponsavel500>
  > & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
