/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type {
  ListarOpcoesAlunosQueryResponse,
  ListarOpcoesAlunos500,
} from "../../types/aluno/ListarOpcoesAlunos.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const listarOpcoesAlunosQueryKey = () =>
  [{ url: "/v1/alunos/options" }] as const;

export type ListarOpcoesAlunosQueryKey = ReturnType<
  typeof listarOpcoesAlunosQueryKey
>;

/**
 * @description Retorna uma lista de opções de alunos.
 * {@link /v1/alunos/options}
 */
export async function listarOpcoesAlunos(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    ListarOpcoesAlunosQueryResponse,
    ResponseErrorConfig<ListarOpcoesAlunos500>,
    unknown
  >({ method: "GET", url: `/v1/alunos/options`, ...requestConfig });
  return res.data;
}

export function listarOpcoesAlunosQueryOptions(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = listarOpcoesAlunosQueryKey();
  return queryOptions<
    ListarOpcoesAlunosQueryResponse,
    ResponseErrorConfig<ListarOpcoesAlunos500>,
    ListarOpcoesAlunosQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return listarOpcoesAlunos({ ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Retorna uma lista de opções de alunos.
 * {@link /v1/alunos/options}
 */
export function useListarOpcoesAlunos<
  TData = ListarOpcoesAlunosQueryResponse,
  TQueryData = ListarOpcoesAlunosQueryResponse,
  TQueryKey extends QueryKey = ListarOpcoesAlunosQueryKey,
>(
  options: {
    query?: Partial<
      QueryObserverOptions<
        ListarOpcoesAlunosQueryResponse,
        ResponseErrorConfig<ListarOpcoesAlunos500>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? listarOpcoesAlunosQueryKey();

  const query = useQuery(
    {
      ...listarOpcoesAlunosQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<ListarOpcoesAlunos500>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
