/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type {
  DeletarAlunoMutationResponse,
  DeletarAlunoPathParams,
  DeletarAluno404,
  DeletarAluno409,
  DeletarAluno500,
} from "../../types/aluno/DeletarAluno.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  UseMutationOptions,
  UseMutationResult,
  QueryClient,
} from "@tanstack/react-query";
import { mutationOptions, useMutation } from "@tanstack/react-query";

export const deletarAlunoMutationKey = () =>
  [{ url: "/v1/alunos/:studentId" }] as const;

export type DeletarAlunoMutationKey = ReturnType<
  typeof deletarAlunoMutationKey
>;

/**
 * @description Deleta um aluno por ID.
 * {@link /v1/alunos/:studentId}
 */
export async function deletarAluno(
  studentId: DeletarAlunoPathParams["studentId"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    DeletarAlunoMutationResponse,
    ResponseErrorConfig<DeletarAluno404 | DeletarAluno409 | DeletarAluno500>,
    unknown
  >({ method: "DELETE", url: `/v1/alunos/${studentId}`, ...requestConfig });
  return res.data;
}

export function deletarAlunoMutationOptions<TContext = unknown>(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const mutationKey = deletarAlunoMutationKey();
  return mutationOptions<
    DeletarAlunoMutationResponse,
    ResponseErrorConfig<DeletarAluno404 | DeletarAluno409 | DeletarAluno500>,
    { studentId: DeletarAlunoPathParams["studentId"] },
    TContext
  >({
    mutationKey,
    mutationFn: async ({ studentId }) => {
      return deletarAluno(studentId, config);
    },
  });
}

/**
 * @description Deleta um aluno por ID.
 * {@link /v1/alunos/:studentId}
 */
export function useDeletarAluno<TContext>(
  options: {
    mutation?: UseMutationOptions<
      DeletarAlunoMutationResponse,
      ResponseErrorConfig<DeletarAluno404 | DeletarAluno409 | DeletarAluno500>,
      { studentId: DeletarAlunoPathParams["studentId"] },
      TContext
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { mutation = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...mutationOptions } = mutation;
  const mutationKey = mutationOptions.mutationKey ?? deletarAlunoMutationKey();

  const baseOptions = deletarAlunoMutationOptions(config) as UseMutationOptions<
    DeletarAlunoMutationResponse,
    ResponseErrorConfig<DeletarAluno404 | DeletarAluno409 | DeletarAluno500>,
    { studentId: DeletarAlunoPathParams["studentId"] },
    TContext
  >;

  return useMutation<
    DeletarAlunoMutationResponse,
    ResponseErrorConfig<DeletarAluno404 | DeletarAluno409 | DeletarAluno500>,
    { studentId: DeletarAlunoPathParams["studentId"] },
    TContext
  >(
    {
      ...baseOptions,
      mutationKey,
      ...mutationOptions,
    },
    queryClient,
  ) as UseMutationResult<
    DeletarAlunoMutationResponse,
    ResponseErrorConfig<DeletarAluno404 | DeletarAluno409 | DeletarAluno500>,
    { studentId: DeletarAlunoPathParams["studentId"] },
    TContext
  >;
}
