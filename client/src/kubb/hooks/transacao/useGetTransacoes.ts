/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/services/api";
import type {
  GetTransacoesQueryResponse,
  GetTransacoesQueryParams,
} from "../../types/GetTransacoes.ts";
import type { RequestConfig, ResponseErrorConfig } from "@/services/api";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getTransacoesQueryKey = (params?: GetTransacoesQueryParams) =>
  [{ url: "/v1/transacoes" }, ...(params ? [params] : [])] as const;

export type GetTransacoesQueryKey = ReturnType<typeof getTransacoesQueryKey>;

/**
 * @description Lista transacoes com paginacao, ordenacao e filtros opcionais.
 * {@link /v1/transacoes}
 */
export async function getTransacoes(
  params?: GetTransacoesQueryParams,
  config: Partial<RequestConfig> & { client?: typeof fetch } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetTransacoesQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({ method: "GET", url: `/v1/transacoes`, params, ...requestConfig });
  return res.data;
}

export function getTransacoesQueryOptions(
  params?: GetTransacoesQueryParams,
  config: Partial<RequestConfig> & { client?: typeof fetch } = {},
) {
  const queryKey = getTransacoesQueryKey(params);
  return queryOptions<
    GetTransacoesQueryResponse,
    ResponseErrorConfig<Error>,
    GetTransacoesQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      config.signal = signal;
      return getTransacoes(params, config);
    },
  });
}

/**
 * @description Lista transacoes com paginacao, ordenacao e filtros opcionais.
 * {@link /v1/transacoes}
 */
export function useGetTransacoes<
  TData = GetTransacoesQueryResponse,
  TQueryData = GetTransacoesQueryResponse,
  TQueryKey extends QueryKey = GetTransacoesQueryKey,
>(
  params?: GetTransacoesQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetTransacoesQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: typeof fetch };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...queryOptions } = queryConfig;
  const queryKey = queryOptions?.queryKey ?? getTransacoesQueryKey(params);

  const query = useQuery(
    {
      ...getTransacoesQueryOptions(params, config),
      queryKey,
      ...queryOptions,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
