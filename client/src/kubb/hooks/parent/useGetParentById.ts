/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type {
  GetParentByIdQueryResponse,
  GetParentByIdPathParams,
  GetParentById400,
} from "../../types/parent/GetParentById.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getParentByIdQueryKey = (
  parentId: GetParentByIdPathParams["parentId"],
) =>
  [{ url: "/v1/parents/:parentId", params: { parentId: parentId } }] as const;

export type GetParentByIdQueryKey = ReturnType<typeof getParentByIdQueryKey>;

/**
 * @description Lista um responsável específico pelo ID.
 * @summary Listar responsável por ID
 * {@link /v1/parents/:parentId}
 */
export async function getParentById(
  parentId: GetParentByIdPathParams["parentId"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetParentByIdQueryResponse,
    ResponseErrorConfig<GetParentById400>,
    unknown
  >({
    method: "GET",
    url: `/v1/parents/${parentId}`,
    baseURL: `http://localhost:8080`,
    ...requestConfig,
  });
  return res.data;
}

export function getParentByIdQueryOptions(
  parentId: GetParentByIdPathParams["parentId"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getParentByIdQueryKey(parentId);
  return queryOptions<
    GetParentByIdQueryResponse,
    ResponseErrorConfig<GetParentById400>,
    GetParentByIdQueryResponse,
    typeof queryKey
  >({
    enabled: !!parentId,
    queryKey,
    queryFn: async ({ signal }) => {
      return getParentById(parentId, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * @description Lista um responsável específico pelo ID.
 * @summary Listar responsável por ID
 * {@link /v1/parents/:parentId}
 */
export function useGetParentById<
  TData = GetParentByIdQueryResponse,
  TQueryData = GetParentByIdQueryResponse,
  TQueryKey extends QueryKey = GetParentByIdQueryKey,
>(
  parentId: GetParentByIdPathParams["parentId"],
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetParentByIdQueryResponse,
        ResponseErrorConfig<GetParentById400>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? getParentByIdQueryKey(parentId);

  const query = useQuery(
    {
      ...getParentByIdQueryOptions(parentId, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<GetParentById400>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
