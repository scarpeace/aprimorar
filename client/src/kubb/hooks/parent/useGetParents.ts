/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type {
  GetParentsQueryResponse,
  GetParentsQueryParams,
  GetParents400,
} from "../../types/parent/GetParents.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getParentsQueryKey = (params?: GetParentsQueryParams) =>
  [{ url: "/v1/parents" }, ...(params ? [params] : [])] as const;

export type GetParentsQueryKey = ReturnType<typeof getParentsQueryKey>;

/**
 * @description Lista todos os responsáveis cadastrados.
 * @summary Listar responsáveis
 * {@link /v1/parents}
 */
export async function getParents(
  params?: GetParentsQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetParentsQueryResponse,
    ResponseErrorConfig<GetParents400>,
    unknown
  >({
    method: "GET",
    url: `/v1/parents`,
    baseURL: `http://localhost:8080`,
    params,
    ...requestConfig,
  });
  return res.data;
}

export function getParentsQueryOptions(
  params?: GetParentsQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getParentsQueryKey(params);
  return queryOptions<
    GetParentsQueryResponse,
    ResponseErrorConfig<GetParents400>,
    GetParentsQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return getParents(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Lista todos os responsáveis cadastrados.
 * @summary Listar responsáveis
 * {@link /v1/parents}
 */
export function useGetParents<
  TData = GetParentsQueryResponse,
  TQueryData = GetParentsQueryResponse,
  TQueryKey extends QueryKey = GetParentsQueryKey,
>(
  params?: GetParentsQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetParentsQueryResponse,
        ResponseErrorConfig<GetParents400>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? getParentsQueryKey(params);

  const query = useQuery(
    {
      ...getParentsQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<GetParents400>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
