/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type { MeQueryResponse } from "../../types/auth/Me.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const meQueryKey = () => [{ url: "/v1/auth/me" }] as const;

export type MeQueryKey = ReturnType<typeof meQueryKey>;

/**
 * {@link /v1/auth/me}
 */
export async function me(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    MeQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "GET",
    url: `/v1/auth/me`,
    baseURL: `http://localhost:8080`,
    ...requestConfig,
  });
  return res.data;
}

export function meQueryOptions(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = meQueryKey();
  return queryOptions<
    MeQueryResponse,
    ResponseErrorConfig<Error>,
    MeQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return me({ ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * {@link /v1/auth/me}
 */
export function useMe<
  TData = MeQueryResponse,
  TQueryData = MeQueryResponse,
  TQueryKey extends QueryKey = MeQueryKey,
>(
  options: {
    query?: Partial<
      QueryObserverOptions<
        MeQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? meQueryKey();

  const query = useQuery(
    {
      ...meQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
