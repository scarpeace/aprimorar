/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/services/api";
import type {
  GetColaboradoresQueryResponse,
  GetColaboradoresQueryParams,
} from "../../types/GetColaboradores.ts";
import type { RequestConfig, ResponseErrorConfig } from "@/services/api";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getColaboradoresQueryKey = (
  params?: GetColaboradoresQueryParams,
) => [{ url: "/v1/colaboradores" }, ...(params ? [params] : [])] as const;

export type GetColaboradoresQueryKey = ReturnType<
  typeof getColaboradoresQueryKey
>;

/**
 * @description Retorna uma lista paginada de colaboradores.
 * {@link /v1/colaboradores}
 */
export async function getColaboradores(
  params?: GetColaboradoresQueryParams,
  config: Partial<RequestConfig> & { client?: typeof fetch } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetColaboradoresQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({ method: "GET", url: `/v1/colaboradores`, params, ...requestConfig });
  return res.data;
}

export function getColaboradoresQueryOptions(
  params?: GetColaboradoresQueryParams,
  config: Partial<RequestConfig> & { client?: typeof fetch } = {},
) {
  const queryKey = getColaboradoresQueryKey(params);
  return queryOptions<
    GetColaboradoresQueryResponse,
    ResponseErrorConfig<Error>,
    GetColaboradoresQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      config.signal = signal;
      return getColaboradores(params, config);
    },
  });
}

/**
 * @description Retorna uma lista paginada de colaboradores.
 * {@link /v1/colaboradores}
 */
export function useGetColaboradores<
  TData = GetColaboradoresQueryResponse,
  TQueryData = GetColaboradoresQueryResponse,
  TQueryKey extends QueryKey = GetColaboradoresQueryKey,
>(
  params?: GetColaboradoresQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetColaboradoresQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: typeof fetch };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...queryOptions } = queryConfig;
  const queryKey = queryOptions?.queryKey ?? getColaboradoresQueryKey(params);

  const query = useQuery(
    {
      ...getColaboradoresQueryOptions(params, config),
      queryKey,
      ...queryOptions,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
