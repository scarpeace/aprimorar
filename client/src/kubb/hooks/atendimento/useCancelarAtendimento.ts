/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type {
  CancelarAtendimentoMutationResponse,
  CancelarAtendimentoPathParams,
} from "../../types/atendimento/CancelarAtendimento.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  UseMutationOptions,
  UseMutationResult,
  QueryClient,
} from "@tanstack/react-query";
import { mutationOptions, useMutation } from "@tanstack/react-query";

export const cancelarAtendimentoMutationKey = () =>
  [{ url: "/v1/atendimentos/:id/cancelar" }] as const;

export type CancelarAtendimentoMutationKey = ReturnType<
  typeof cancelarAtendimentoMutationKey
>;

/**
 * @description Muda o status de um atendimento para CANCELADO.
 * {@link /v1/atendimentos/:id/cancelar}
 */
export async function cancelarAtendimento(
  id: CancelarAtendimentoPathParams["id"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    CancelarAtendimentoMutationResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "PATCH",
    url: `/v1/atendimentos/${id}/cancelar`,
    ...requestConfig,
  });
  return res.data;
}

export function cancelarAtendimentoMutationOptions<TContext = unknown>(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const mutationKey = cancelarAtendimentoMutationKey();
  return mutationOptions<
    CancelarAtendimentoMutationResponse,
    ResponseErrorConfig<Error>,
    { id: CancelarAtendimentoPathParams["id"] },
    TContext
  >({
    mutationKey,
    mutationFn: async ({ id }) => {
      return cancelarAtendimento(id, config);
    },
  });
}

/**
 * @description Muda o status de um atendimento para CANCELADO.
 * {@link /v1/atendimentos/:id/cancelar}
 */
export function useCancelarAtendimento<TContext>(
  options: {
    mutation?: UseMutationOptions<
      CancelarAtendimentoMutationResponse,
      ResponseErrorConfig<Error>,
      { id: CancelarAtendimentoPathParams["id"] },
      TContext
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { mutation = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...mutationOptions } = mutation;
  const mutationKey =
    mutationOptions.mutationKey ?? cancelarAtendimentoMutationKey();

  const baseOptions = cancelarAtendimentoMutationOptions(
    config,
  ) as UseMutationOptions<
    CancelarAtendimentoMutationResponse,
    ResponseErrorConfig<Error>,
    { id: CancelarAtendimentoPathParams["id"] },
    TContext
  >;

  return useMutation<
    CancelarAtendimentoMutationResponse,
    ResponseErrorConfig<Error>,
    { id: CancelarAtendimentoPathParams["id"] },
    TContext
  >(
    {
      ...baseOptions,
      mutationKey,
      ...mutationOptions,
    },
    queryClient,
  ) as UseMutationResult<
    CancelarAtendimentoMutationResponse,
    ResponseErrorConfig<Error>,
    { id: CancelarAtendimentoPathParams["id"] },
    TContext
  >;
}
