/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type {
  GetAtendimentosQueryResponse,
  GetAtendimentosQueryParams,
} from "../../types/atendimento/GetAtendimentos.ts";
import type {
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getAtendimentosQueryKey = (params?: GetAtendimentosQueryParams) =>
  [{ url: "/v1/atendimentos" }, ...(params ? [params] : [])] as const;

export type GetAtendimentosQueryKey = ReturnType<
  typeof getAtendimentosQueryKey
>;

/**
 * @description Lista atendimentos com paginacao, ordenacao e filtros opcionais.
 * {@link /v1/atendimentos}
 */
export async function getAtendimentos(
  params?: GetAtendimentosQueryParams,
  config: Partial<RequestConfig> & { client?: typeof fetch } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetAtendimentosQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({ method: "GET", url: `/v1/atendimentos`, params, ...requestConfig });
  return res.data;
}

export function getAtendimentosQueryOptions(
  params?: GetAtendimentosQueryParams,
  config: Partial<RequestConfig> & { client?: typeof fetch } = {},
) {
  const queryKey = getAtendimentosQueryKey(params);
  return queryOptions<
    GetAtendimentosQueryResponse,
    ResponseErrorConfig<Error>,
    GetAtendimentosQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      config.signal = signal;
      return getAtendimentos(params, config);
    },
  });
}

/**
 * @description Lista atendimentos com paginacao, ordenacao e filtros opcionais.
 * {@link /v1/atendimentos}
 */
export function useGetAtendimentos<
  TData = GetAtendimentosQueryResponse,
  TQueryData = GetAtendimentosQueryResponse,
  TQueryKey extends QueryKey = GetAtendimentosQueryKey,
>(
  params?: GetAtendimentosQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetAtendimentosQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: typeof fetch };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...queryOptions } = queryConfig;
  const queryKey = queryOptions?.queryKey ?? getAtendimentosQueryKey(params);

  const query = useQuery(
    {
      ...getAtendimentosQueryOptions(params, config),
      queryKey,
      ...queryOptions,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
