{
  "required": ["colaboradorId", "dataRepasse", "formaPagamento", "loteId", "quantidadeRepasses", "repasses", "valorTotal"],
  "type": "object",
  "properties": {
    "loteId": { "type": "string", "format": "uuid" },
    "colaboradorId": { "type": "string", "format": "uuid" },
    "dataRepasse": { "type": "string", "format": "date-time" },
    "formaPagamento": {
      "type": "string",
      "description": "Forma de pagamento",
      "enum": ["PIX", "DINHEIRO", "CARTAO_CREDITO", "CARTAO_DEBITO", "BOLETO", "TRANSFERENCIA"]
    },
    "comprovanteUrl": { "type": "string", "nullable": true },
    "valorTotal": { "type": "number" },
    "quantidadeRepasses": { "type": "integer", "format": "int64" },
    "repasses": {
      "type": "array",
      "items": {
        "required": ["atendimentoId", "id", "status", "valor"],
        "type": "object",
        "properties": {
          "id": { "type": "integer", "format": "int64" },
          "atendimentoId": { "type": "integer", "format": "int64" },
          "valor": { "type": "number" },
          "status": { "type": "string", "enum": ["PENDENTE", "PAGO", "CANCELADO"] }
        },
        "description": "Repasse individual pertencente a um lote de pagamento",
        "x-readme-ref-name": "RepasseLoteItem"
      }
    }
  },
  "description": "Detalhes de um lote de repasses pagos",
  "x-readme-ref-name": "RepasseLoteDetalheResponse"
}
