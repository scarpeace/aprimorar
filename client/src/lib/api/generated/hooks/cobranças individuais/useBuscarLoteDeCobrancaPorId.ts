/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/api/kubb-client";
import type {
  BuscarLoteDeCobrancaPorIdQueryResponse,
  BuscarLoteDeCobrancaPorIdPathParams,
  BuscarLoteDeCobrancaPorId401,
  BuscarLoteDeCobrancaPorId404,
  BuscarLoteDeCobrancaPorId500,
} from "../../types/BuscarLoteDeCobrancaPorId.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@/lib/api/kubb-client";
import type { QueryKey, QueryClient, QueryObserverOptions, UseQueryResult } from "@tanstack/react-query";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const buscarLoteDeCobrancaPorIdQueryKey = (loteId: BuscarLoteDeCobrancaPorIdPathParams["loteId"]) =>
  [{ url: "/financeiro/cobrancas/lotes/:loteId", params: { loteId: loteId } }] as const;

export type BuscarLoteDeCobrancaPorIdQueryKey = ReturnType<typeof buscarLoteDeCobrancaPorIdQueryKey>;

/**
 * @description Busca os detalhes de um lote de cobranças pelo identificador.
 * {@link /financeiro/cobrancas/lotes/:loteId}
 */
export async function buscarLoteDeCobrancaPorId(
  loteId: BuscarLoteDeCobrancaPorIdPathParams["loteId"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    BuscarLoteDeCobrancaPorIdQueryResponse,
    ResponseErrorConfig<BuscarLoteDeCobrancaPorId401 | BuscarLoteDeCobrancaPorId404 | BuscarLoteDeCobrancaPorId500>,
    unknown
  >({ method: "GET", url: `/financeiro/cobrancas/lotes/${loteId}`, ...requestConfig });
  return res.data;
}

export function buscarLoteDeCobrancaPorIdQueryOptions(
  loteId: BuscarLoteDeCobrancaPorIdPathParams["loteId"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = buscarLoteDeCobrancaPorIdQueryKey(loteId);
  return queryOptions<
    BuscarLoteDeCobrancaPorIdQueryResponse,
    ResponseErrorConfig<BuscarLoteDeCobrancaPorId401 | BuscarLoteDeCobrancaPorId404 | BuscarLoteDeCobrancaPorId500>,
    BuscarLoteDeCobrancaPorIdQueryResponse,
    typeof queryKey
  >({
    enabled: !!loteId,
    queryKey,
    queryFn: async ({ signal }) => {
      return buscarLoteDeCobrancaPorId(loteId, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Busca os detalhes de um lote de cobranças pelo identificador.
 * {@link /financeiro/cobrancas/lotes/:loteId}
 */
export function useBuscarLoteDeCobrancaPorId<
  TData = BuscarLoteDeCobrancaPorIdQueryResponse,
  TQueryData = BuscarLoteDeCobrancaPorIdQueryResponse,
  TQueryKey extends QueryKey = BuscarLoteDeCobrancaPorIdQueryKey,
>(
  loteId: BuscarLoteDeCobrancaPorIdPathParams["loteId"],
  options: {
    query?: Partial<
      QueryObserverOptions<
        BuscarLoteDeCobrancaPorIdQueryResponse,
        ResponseErrorConfig<BuscarLoteDeCobrancaPorId401 | BuscarLoteDeCobrancaPorId404 | BuscarLoteDeCobrancaPorId500>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? buscarLoteDeCobrancaPorIdQueryKey(loteId);

  const query = useQuery(
    {
      ...buscarLoteDeCobrancaPorIdQueryOptions(loteId, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<
    TData,
    ResponseErrorConfig<BuscarLoteDeCobrancaPorId401 | BuscarLoteDeCobrancaPorId404 | BuscarLoteDeCobrancaPorId500>
  > & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
