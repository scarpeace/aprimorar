/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/api/kubb-client";
import type { GetCurrentUserQueryResponse } from "../../types/GetCurrentUser.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@/lib/api/kubb-client";
import type { QueryKey, QueryClient, QueryObserverOptions, UseQueryResult } from "@tanstack/react-query";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getCurrentUserQueryKey = () => [{ url: "/auth/me" }] as const;

export type GetCurrentUserQueryKey = ReturnType<typeof getCurrentUserQueryKey>;

/**
 * @description Retorna os dados do usuário autenticado.
 * {@link /auth/me}
 */
export async function getCurrentUser(config: Partial<RequestConfig> & { client?: Client } = {}) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<GetCurrentUserQueryResponse, ResponseErrorConfig<Error>, unknown>({
    method: "GET",
    url: `/auth/me`,
    ...requestConfig,
  });
  return res.data;
}

export function getCurrentUserQueryOptions(config: Partial<RequestConfig> & { client?: Client } = {}) {
  const queryKey = getCurrentUserQueryKey();
  return queryOptions<GetCurrentUserQueryResponse, ResponseErrorConfig<Error>, GetCurrentUserQueryResponse, typeof queryKey>({
    queryKey,
    queryFn: async ({ signal }) => {
      return getCurrentUser({ ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Retorna os dados do usuário autenticado.
 * {@link /auth/me}
 */
export function useGetCurrentUser<
  TData = GetCurrentUserQueryResponse,
  TQueryData = GetCurrentUserQueryResponse,
  TQueryKey extends QueryKey = GetCurrentUserQueryKey,
>(
  options: {
    query?: Partial<
      QueryObserverOptions<GetCurrentUserQueryResponse, ResponseErrorConfig<Error>, TData, TQueryData, TQueryKey>
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? getCurrentUserQueryKey();

  const query = useQuery(
    {
      ...getCurrentUserQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
