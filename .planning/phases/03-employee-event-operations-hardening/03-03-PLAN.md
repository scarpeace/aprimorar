---
phase: 03-employee-event-operations-hardening
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified: [
  "client/src/features/employees/pages/EmployeesListPage.tsx",
  "client/src/features/employees/pages/EmployeeDetailsPage.tsx"
]
autonomous: true
requirements: [EMPL-02, EMPL-04]
must_haves:
  truths:
    - "A listagem de funcionários exibe a descrição amigável da função (ex: 'Professor' em vez de 'TEACHER')."
    - "Os detalhes do funcionário exibem a descrição amigável da função."
  artifacts:
    - path: "client/src/features/employees/pages/EmployeesListPage.tsx"
      provides: "Listagem de funcionários com labels amigáveis"
  key_links:
    - from: "EmployeesListPage.tsx"
      to: "dutyLabels.ts"
      via: "label mapping"
---

<objective>
Fortalecer a interface de funcionários com descrições amigáveis dos papéis/funções.

Finalidade: Melhorar a legibilidade das funções dos funcionários para a secretária.
Saída: Componentes de lista e detalhes atualizados.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-employee-event-operations-hardening/03-02-SUMMARY.md
@client/src/features/employees/utils/dutyLabels.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar UI de Funcionários com Labels</name>
  <files>
    client/src/features/employees/pages/EmployeesListPage.tsx,
    client/src/features/employees/pages/EmployeeDetailsPage.tsx
  </files>
  <action>
    - Importar 'dutyLabels' de '../utils/dutyLabels'.
    - Na tabela de funcionários e na tela de detalhes, substituir a exibição bruta do enum (e.g. employee.duty) pela tradução amigável (e.g. dutyLabels[employee.duty]) (D-02).
  </action>
  <verify>
    <automated>cd client && npm run typecheck</automated>
  </verify>
  <done>Labels amigáveis aplicadas nas telas de funcionários.</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Exibição de funções dos funcionários em português.</what-built>
  <how-to-verify>
    1. Acessar a listagem de funcionários.
    2. Verificar se a coluna de função exibe "Professor", "Administrativo", etc.
    3. Entrar nos detalhes de um funcionário e verificar se o papel está traduzido.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI Display | Exibição de dados pré-validados pelo backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Spoofing | Duty Labels | mitigate | Usar fallback para valores de enum desconhecidos no mapeamento de labels. |
</threat_model>

<verification>
Navegar pelo frontend e garantir que nenhum valor de enum bruto (ALL_CAPS) seja exibido para o usuário final nas telas de funcionários.
</verification>

<success_criteria>
Interface de funcionários utilizando terminologia amigável em português.
</success_criteria>

<output>
After completion, create `.planning/phases/03-employee-event-operations-hardening/03-03-SUMMARY.md`
</output>
