---
phase: "01"
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - client/src/features/events/components/EventsTable.tsx
  - client/src/features/employees/pages/EmployeeDetailPage.tsx
autonomous: true
requirements: [TAB-01, TAB-02]

must_haves:
  truths:
    - "Searching for a non-existent student name shows 'Nenhum atendimento encontrado' inside the table body"
    - "Typing in the search bar triggers a filtered request after 500ms delay"
    - "Table headers remain visible during empty search results"
    - "Events are initially loaded with startDate in descending order"
  artifacts:
    - path: "client/src/features/employees/pages/EmployeeDetailPage.tsx"
      provides: "Debounced search logic and ListSearchInput integration"
    - path: "client/src/features/events/components/EventsTable.tsx"
      provides: "Inline empty state handling"
  key_links:
    - from: "EmployeeDetailPage.tsx"
      to: "ListSearchInput"
      via: "searchTerm state binding"
---

<objective>
Implement the search UI and pagination logic on the employee detail page, including debouncing and inline empty states.

Purpose: Allow users to efficiently find specific events for an employee.
Output: Updated EmployeeDetailPage and EventsTable with search and pagination support.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-employee-events-tracking/01-CONTEXT.md
@.planning/phases/01-employee-events-tracking/01-PATTERNS.md
@client/src/features/employees/pages/EmployeeDetailPage.tsx
@client/src/features/events/components/EventsTable.tsx
@client/src/components/ui/list-search-input.tsx
@client/src/lib/shared/use-debounce.ts
</context>

<tasks>

<task type="auto">
  <name>Implement Inline Empty State in EventsTable</name>
  <files>client/src/features/events/components/EventsTable.tsx</files>
  <read_first>
    - client/src/features/events/components/EventsTable.tsx
    - .planning/phases/01-employee-events-tracking/01-CONTEXT.md (D-04)
    - .planning/phases/01-employee-events-tracking/01-PATTERNS.md (Table Empty State pattern)
  </read_first>
  <action>
    Modify the `tbody` of `EventsTable` to handle empty results by displaying an inline message instead of just an empty list.

    Implementation:
    ```tsx
    <tbody className="whitespace-nowrap">
      {eventsPage?.content?.length === 0 ? (
        <tr>
          <td colSpan={8} className="text-center py-8 text-base-content/50">
            Nenhum atendimento encontrado.
          </td>
        </tr>
      ) : (
        eventsPage?.content?.map((event) => (
          // ... existing tr content ...
        ))
      )}
    </tbody>
    ```
  </action>
  <verify>
    <automated>grep "Nenhum atendimento encontrado" client/src/features/events/components/EventsTable.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `EventsTable.tsx` contains a check for `eventsPage?.content?.length === 0`.
    - It renders a `<tr>` with `colSpan={8}` when empty.
    - The message matches "Nenhum atendimento encontrado" per D-04.
  </acceptance_criteria>
  <done>Inline empty state implemented in EventsTable.</done>
</task>

<task type="auto">
  <name>Integrate Debounced Search in EmployeeDetailPage</name>
  <files>client/src/features/employees/pages/EmployeeDetailPage.tsx</files>
  <read_first>
    - client/src/features/employees/pages/EmployeeDetailPage.tsx
    - client/src/components/ui/list-search-input.tsx
    - client/src/lib/shared/use-debounce.ts
    - .planning/phases/01-employee-events-tracking/01-CONTEXT.md (D-01, D-02, D-03)
  </read_first>
  <action>
    1. Import `ListSearchInput` and `useDebounce`.
    2. Add `searchTerm` state: `const [searchTerm, setSearchTerm] = useState("");`.
    3. Add `debouncedSearchTerm`: `const debouncedSearchTerm = useDebounce(searchTerm, 500);`.
    4. Place `ListSearchInput` above the `EventsTable` inside the "Atendimentos" `SectionCard`. Use `placeholder="Buscar aluno"` and bind to `searchTerm`.
    5. Update `useGetEventsByEmployeeId` call:
       ```tsx
       const employeeEventsQuery = useGetEventsByEmployeeId(employeeId, {
         page: currentPage,
         studentName: debouncedSearchTerm,
         sort: ["startDate,desc"]
       });
       ```
    6. Ensure `onPageChange` resets the `currentPage` state when the search term changes (optional but good practice).
  </action>
  <verify>
    <automated>grep "useDebounce(searchTerm, 500)" client/src/features/employees/pages/EmployeeDetailPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `EmployeeDetailPage.tsx` uses `useDebounce`.
    - `ListSearchInput` is present with `placeholder="Buscar aluno"`.
    - `useGetEventsByEmployeeId` is called with `{ page: currentPage, studentName: debouncedSearchTerm, sort: ["startDate,desc"] }`.
  </acceptance_criteria>
  <done>Debounced search and pagination integrated on employee detail page.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI → State | Search input state management |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Denial of Service | EmployeeDetailPage.tsx | mitigate | Use `useDebounce` to limit the rate of API requests sent to the backend. |
</threat_model>

<verification>
Manual verification:
1. Navigate to an employee's detail page.
2. Verify events are sorted by date descending.
3. Type a student name in the search bar.
4. Verify results filter after a brief pause.
5. Verify "Nenhum atendimento encontrado" appears for non-existent students.
</verification>

<success_criteria>
- User can search events by student name.
- Search is debounced (500ms).
- Table shows inline empty state message.
- Events are sorted by startDate descending.
</success_criteria>

<output>
After completion, create `.planning/phases/01-employee-events-tracking/01-02-SUMMARY.md`
</output>
