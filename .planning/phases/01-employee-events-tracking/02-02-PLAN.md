---
phase: "02"
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - client/src/features/employees/components/EmployeeEventsTable.tsx
autonomous: true
requirements: [TAB-03, TAB-04, TAB-05]

must_haves:
  truths:
    - "EmployeeEventsTable contains a toggle to hide paid events"
    - "Each row has an action to mark as paid if unpaid"
    - "Payment date is displayed for paid events"
  artifacts:
    - path: "client/src/features/employees/components/EmployeeEventsTable.tsx"
      provides: "Interactive payment workflow UI"
---

<objective>
Implement the frontend UI for the payment workflow, including filtering and payment actions.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-employee-events-tracking/02-RESEARCH.md
@client/src/features/employees/components/EmployeeEventsTable.tsx
</context>

<tasks>

<task type="auto">
  <name>Synchronize Frontend Hooks</name>
  <files>client/src/kubb/index.ts</files>
  <action>
    Run `npm run sync` in the `client` directory to pick up the backend changes (new fields and `hidePaid` parameter).
  </action>
  <verify>
    <automated>grep "hidePaid" client/src/kubb/gen/hooks/useGetEventsByEmployeeId.ts</automated>
  </verify>
  <done>Hooks synchronized with updated API.</done>
</task>

<task type="auto">
  <name>Implement Hide Paid Toggle</name>
  <files>client/src/features/employees/components/EmployeeEventsTable.tsx</files>
  <action>
    1. Add `hidePaid` state (boolean, default false).
    2. Add a `ToggleSwitch` or checkbox labeled "Ocultar Pagos" in the `SectionCard` headerActions.
    3. Update `useGetEventsByEmployeeId` to pass `hidePaid` to the API.
  </action>
  <verify>
    <automated>grep "hidePaid" client/src/features/employees/components/EmployeeEventsTable.tsx</automated>
  </verify>
  <done>Status filtering toggle implemented.</done>
</task>

<task type="auto">
  <name>Add Payment Action to Table</name>
  <files>client/src/features/employees/components/EmployeeEventsTable.tsx</files>
  <action>
    1. Use `useSettleEmployeePaymentEvent` mutation hook.
    2. In the table rows:
       - If `event.employeePaid` is false: Show a "Pagar" button (icon + text or just icon).
       - Clicking the button calls the mutation with `paid: true`.
       - If `event.employeePaid` is true: Show the `event.employeePaymentDate` formatted.
    3. Ensure table remains clickable for details, but the button has `e.stopPropagation()`.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Interactive payment actions added to the table.</done>
</task>

</tasks>

<verification>
1. Start the app and verify the toggle works.
2. Mark an event as paid and ensure it disappears when "Ocultar Pagos" is active.
</verification>
