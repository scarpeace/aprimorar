---
phase: 04-financial-tracking-core
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified: [
  client/src/features/finance/pages/GeneralExpensesPage.tsx,
  client/src/features/finance/components/GeneralExpenseForm.tsx,
  client/src/components/layout/MainLayout.tsx,
  client/src/App.tsx
]
autonomous: true
requirements: [FIN-01]
must_haves:
  truths:
    - "Finance menu item visible in sidebar"
    - "General Expenses page displays a list of expenses"
    - "General Expense creation form uses standard project components"
  artifacts:
    - path: "client/src/features/finance/pages/GeneralExpensesPage.tsx"
      provides: "List and CRUD for general expenses"
    - path: "client/src/components/layout/MainLayout.tsx"
      provides: "Sidebar navigation entry"
---

<objective>
Implement the frontend infrastructure for finance and the General Expenses management module.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-financial-tracking-core/04-02-PLAN.md
@client/src/components/layout/MainLayout.tsx
</context>

<tasks>

<task type="auto">
  <name>Regenerate API Clients</name>
  <files>client/src/kubb/index.ts</files>
  <action>
    Run `npm run kubb` in the client directory to regenerate types and hooks for the new finance and settlement endpoints.
  </action>
  <verify>
    <automated>ls client/src/kubb/gen/hooks/useGetFinanceSummary.ts</automated>
  </verify>
  <done>Frontend types sync with backend API.</done>
</task>

<task type="auto">
  <name>Navigation and Layout</name>
  <files>client/src/components/layout/MainLayout.tsx, client/src/App.tsx</files>
  <action>
    - Add "Financeiro" link to the MainLayout sidebar (use a relevant icon like BanknotesIcon).
    - Register routes in App.tsx: /finance/dashboard, /finance/expenses, /finance/settlement.
  </action>
  <verify>
    <automated>grep "Financeiro" client/src/components/layout/MainLayout.tsx</automated>
  </verify>
  <done>Finance module is accessible via UI.</done>
</task>

<task type="auto">
  <name>General Expenses Page</name>
  <files>
    client/src/features/finance/pages/GeneralExpensesPage.tsx,
    client/src/features/finance/components/GeneralExpenseForm.tsx
  </files>
  <action>
    Implement GeneralExpensesPage.
    - List with search and filters (category, date range).
    - Modal form for creating/editing expenses.
    - Use use-mask-input for currency fields (BRL) as per PATTERNS.md.
  </action>
  <verify>
    <automated>ls client/src/features/finance/pages/GeneralExpensesPage.tsx</automated>
  </verify>
  <done>General Expenses management is functional.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | Expense Form | mitigate | Use strict Zod validation for amount (must be positive). |
</threat_model>

<success_criteria>
- Navigation to Finance module works.
- General expenses can be listed, created, and updated.
</success_criteria>

<output>
After completion, create `.planning/phases/04-financial-tracking-core/04-03-SUMMARY.md`
</output>
