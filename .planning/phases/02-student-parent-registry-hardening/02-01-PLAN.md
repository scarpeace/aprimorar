---
phase: 02-student-parent-registry-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/api-aprimorar/src/main/resources/db/migration/V3__student_responsible_links.sql
  - server/api-aprimorar/src/main/java/com/aprimorar/api/domain/student/Student.java
  - server/api-aprimorar/src/main/java/com/aprimorar/api/domain/student/StudentResponsible.java
  - server/api-aprimorar/src/main/java/com/aprimorar/api/domain/student/StudentResponsibleId.java
  - server/api-aprimorar/src/test/java/com/aprimorar/api/domain/student/StudentTest.java
autonomous: true
requirements:
  - STUD-01
  - STUD-02
  - STUD-03
  - PARN-04
must_haves:
  truths:
    - Student persistence supports one or more linked responsáveis instead of a single foreign key.
    - A student record cannot exist without at least one linked responsável.
    - Existing student-parent links migrate into the new relationship model without losing history.
  artifacts:
    - path: server/api-aprimorar/src/main/resources/db/migration/V3__student_responsible_links.sql
      provides: join-table migration and backfill from the legacy `parent_id` column.
      contains: tb_student_responsibles
    - path: server/api-aprimorar/src/main/java/com/aprimorar/api/domain/student/StudentResponsible.java
      provides: relationship entity with optional relationship type.
      contains: relationshipType
    - path: server/api-aprimorar/src/main/java/com/aprimorar/api/domain/student/Student.java
      provides: aggregate rules that enforce at least one linked responsável.
      contains: responsibles
  key_links:
    - from: server/api-aprimorar/src/main/resources/db/migration/V3__student_responsible_links.sql
      to: server/api-aprimorar/src/main/java/com/aprimorar/api/domain/student/StudentResponsible.java
      via: mirrored table/entity mapping
      pattern: tb_student_responsibles
    - from: server/api-aprimorar/src/main/java/com/aprimorar/api/domain/student/Student.java
      to: server/api-aprimorar/src/main/java/com/aprimorar/api/domain/student/StudentResponsible.java
      via: aggregate-owned relationship collection
      pattern: StudentResponsible
---

<objective>
Replace the legacy single-parent persistence shape with a student↔responsável link model that preserves registry integrity.

Purpose: handle the hardest irreversible Phase 2 constraint first so later service and UI work can rely on a stable relationship model.
Output: migrated join-table storage, domain guardrails for linked responsáveis, and tests that lock in the new invariants.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-student-parent-registry-hardening/02-CONTEXT.md
@server/api-aprimorar/src/main/resources/db/migration/V1__init.sql
@server/api-aprimorar/src/main/java/com/aprimorar/api/domain/student/Student.java
@server/api-aprimorar/src/test/java/com/aprimorar/api/domain/student/StudentTest.java

<interfaces>
From `server/api-aprimorar/src/main/java/com/aprimorar/api/domain/student/Student.java`:

```java
public class Student extends BaseEntity {
    private Parent parent;

    public void updateDetails(..., Parent parent, Address address) { ... }
}
```

Replace the direct `Parent parent` ownership with an aggregate-owned link collection because D-04 requires one student to support one or more responsíveis, and D-06 requires metadata on each link.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock in multi-responsável domain invariants with tests</name>
  <files>server/api-aprimorar/src/test/java/com/aprimorar/api/domain/student/StudentTest.java</files>
  <read_first>server/api-aprimorar/src/test/java/com/aprimorar/api/domain/parent/ParentTest.java, server/api-aprimorar/src/main/java/com/aprimorar/api/domain/student/Student.java</read_first>
  <behavior>
    - Test 1: creating a student with zero responsíveis fails with a Portuguese domain exception per D-02.
    - Test 2: replacing links keeps one-or-more responsíveis and preserves optional `relationshipType` values per D-04 and D-06.
    - Test 3: removing the last linked responsável is rejected per D-08.
  </behavior>
  <action>Expand `StudentTest` so the aggregate is specified around `responsibles` instead of a single `parent`. Use deterministic UUID fixtures and explicit assertions for the invariants above. Keep the domain language in Portuguese, and make the tests describe the real guardrails executors must preserve when archive/edit flows later call into this aggregate.</action>
  <acceptance_criteria>`StudentTest` fails against the legacy single-parent model and clearly documents the one-or-more responsável invariant plus optional relationship metadata handling.</acceptance_criteria>
  <verify>
    <automated>test -f server/api-aprimorar/src/test/java/com/aprimorar/api/domain/student/StudentTest.java</automated>
  </verify>
  <done>The new relationship rules are specified before the entity and migration work begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the join-table migration and aggregate-owned relationship model</name>
  <files>server/api-aprimorar/src/main/resources/db/migration/V3__student_responsible_links.sql, server/api-aprimorar/src/main/java/com/aprimorar/api/domain/student/Student.java, server/api-aprimorar/src/main/java/com/aprimorar/api/domain/student/StudentResponsible.java, server/api-aprimorar/src/main/java/com/aprimorar/api/domain/student/StudentResponsibleId.java, server/api-aprimorar/src/test/java/com/aprimorar/api/domain/student/StudentTest.java</files>
  <read_first>server/api-aprimorar/src/main/resources/db/migration/V1__init.sql, server/api-aprimorar/src/main/java/com/aprimorar/api/domain/parent/Parent.java, server/api-aprimorar/src/test/java/com/aprimorar/api/domain/student/StudentTest.java</read_first>
  <behavior>
    - Test 1: the migrated ghost student still keeps a valid ghost responsável link after backfill.
    - Test 2: `Student` owns a collection of links and rejects empty collections on create/update per D-02 and D-08.
  </behavior>
  <action>Create `V3__student_responsible_links.sql` that (1) creates `tb_student_responsibles` with composite key `(student_id, parent_id)`, nullable `relationship_type`, and indexes on both foreign keys, (2) backfills every existing `tb_students.parent_id` into the new table, including the ghost records, and only then (3) drops the legacy `parent_id` column + index from `tb_students`. Implement `StudentResponsibleId` and `StudentResponsible` as the explicit link entity instead of `@ManyToMany` so optional metadata and integrity rules stay first-class. Refactor `Student` to own a `Set<StudentResponsible>` with helper methods that replace links atomically, reject duplicate responsáveis, and throw when a change would leave the student without any links, per D-04, D-06, D-08, and D-09. Do not introduce primary/financial flags per D-05.</action>
  <acceptance_criteria>The database schema no longer relies on `tb_students.parent_id`; every legacy link is backfilled into `tb_student_responsibles`; the `Student` aggregate exposes one-or-more relationship ownership through link objects rather than a single `Parent` field.</acceptance_criteria>
  <verify>
    <automated>cd server/api-aprimorar &amp;&amp; ./mvnw -Dtest=StudentTest test</automated>
  </verify>
  <done>The backend has a migrated persistence foundation that later service and UI plans can build on without revisiting the data model.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flyway migration → production data | historical student-parent links are transformed into the new join-table shape |
| service/domain → database relationship rows | registry integrity depends on aggregate rules matching persisted links |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | `V3__student_responsible_links.sql` | mitigate | Backfill legacy `parent_id` values into `tb_student_responsibles` before dropping the old column, and preserve ghost links explicitly so migration does not tamper with historical references. |
| T-02-02 | I | `Student` aggregate | mitigate | Enforce one-or-more linked responsáveis inside the entity so later service or UI paths cannot persist a student without any responsible links. |
</threat_model>

<verification>
- `cd server/api-aprimorar && ./mvnw -Dtest=StudentTest test`
- Review `V3__student_responsible_links.sql` to confirm legacy rows are backfilled before `parent_id` is removed.
</verification>

<success_criteria>
- The storage model supports multiple responsíveis per student.
- No student can be persisted without at least one linked responsável.
- Existing registry links survive the migration without manual data repair.
</success_criteria>

<output>
After completion, create `.planning/phases/02-student-parent-registry-hardening/02-student-parent-registry-hardening-01-SUMMARY.md`
</output>
