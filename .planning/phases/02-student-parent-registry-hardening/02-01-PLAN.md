---
phase: 02-student-parent-registry-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/api-aprimorar/src/main/java/com/aprimorar/api/domain/student/Student.java
  - server/api-aprimorar/src/main/java/com/aprimorar/api/domain/student/repository/StudentRepository.java
  - server/api-aprimorar/src/test/java/com/aprimorar/api/domain/student/StudentTest.java
autonomous: true
requirements:
  - STUD-01
  - STUD-02
  - STUD-03
  - PARN-04
must_haves:
  truths:
    - Student persistence keeps the current single required responsável relation explicit and protected.
    - A student record cannot exist without exactly one linked responsável.
    - Student read paths can load the linked responsável reliably enough for downstream DTO and SPA work.
  artifacts:
    - path: server/api-aprimorar/src/main/java/com/aprimorar/api/domain/student/Student.java
      provides: aggregate rules that enforce one required responsável.
      contains: parent
    - path: server/api-aprimorar/src/main/java/com/aprimorar/api/domain/student/repository/StudentRepository.java
      provides: fetch strategy for loading the linked responsável on student read paths.
      contains: EntityGraph
  key_links:
    - from: server/api-aprimorar/src/main/java/com/aprimorar/api/domain/student/Student.java
      to: server/api-aprimorar/src/main/java/com/aprimorar/api/domain/student/repository/StudentRepository.java
      via: required `parent` relation used by read queries
      pattern: parent
---

<objective>
Harden the current single-responsável persistence model so later Phase 2 work can rely on stable invariants and predictable reads.

Purpose: lock scope back to the existing relationship shape before changing contracts or UI, and remove the now-rejected many-to-many migration path from execution.
Output: domain guardrails for the required responsável relation, reliable student fetch behavior, and tests that document those invariants.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-student-parent-registry-hardening/02-CONTEXT.md
@server/api-aprimorar/src/main/java/com/aprimorar/api/domain/student/Student.java
@server/api-aprimorar/src/main/java/com/aprimorar/api/domain/student/repository/StudentRepository.java
@server/api-aprimorar/src/test/java/com/aprimorar/api/domain/student/StudentTest.java

<interfaces>
From `server/api-aprimorar/src/main/java/com/aprimorar/api/domain/student/Student.java`:

```java
public class Student extends BaseEntity {
    private Parent parent;

    public void updateDetails(..., Parent parent, Address address) { ... }
}
```

Keep the direct `Parent parent` ownership because D-04 now preserves the current one-student-to-one-responsável model. Strengthen the aggregate and repository behavior around that constraint instead of replacing it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock in the single required responsável invariant with tests</name>
  <files>server/api-aprimorar/src/test/java/com/aprimorar/api/domain/student/StudentTest.java</files>
  <read_first>server/api-aprimorar/src/test/java/com/aprimorar/api/domain/parent/ParentTest.java, server/api-aprimorar/src/main/java/com/aprimorar/api/domain/student/Student.java</read_first>
  <behavior>
    - Test 1: creating a student without responsável fails with a Portuguese domain exception per D-02.
    - Test 2: updating a student with `parent = null` is rejected per D-08.
    - Test 3: valid construction and update preserve the linked responsável reference per D-04.
  </behavior>
  <action>Expand `StudentTest` around the existing `parent` relation instead of replacing it. Use deterministic UUID fixtures and explicit assertions for the required-responsável guardrails that later service and UI work must preserve. Keep domain language in Portuguese and make the tests read like durable business rules, not migration scaffolding.</action>
  <acceptance_criteria>`StudentTest` clearly documents that a student always requires exactly one responsável and that normal updates preserve this invariant.</acceptance_criteria>
  <verify>
    <automated>test -f server/api-aprimorar/src/test/java/com/aprimorar/api/domain/student/StudentTest.java</automated>
  </verify>
  <done>The current relationship rules are specified before service and contract hardening begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement aggregate and repository hardening for the existing responsável link</name>
  <files>server/api-aprimorar/src/main/java/com/aprimorar/api/domain/student/Student.java, server/api-aprimorar/src/main/java/com/aprimorar/api/domain/student/repository/StudentRepository.java, server/api-aprimorar/src/test/java/com/aprimorar/api/domain/student/StudentTest.java</files>
  <read_first>server/api-aprimorar/src/main/java/com/aprimorar/api/domain/parent/Parent.java, server/api-aprimorar/src/main/java/com/aprimorar/api/domain/student/repository/StudentRepository.java, server/api-aprimorar/src/test/java/com/aprimorar/api/domain/student/StudentTest.java</read_first>
  <behavior>
    - Test 1: `Student` rejects missing responsável on create/update per D-02 and D-08.
    - Test 2: student read queries load the linked responsável intentionally so downstream mapping does not depend on accidental lazy access.
  </behavior>
  <action>Keep the existing `parent_id` schema and `Parent parent` entity mapping. Refactor only as needed to make the invariant and fetch behavior explicit: keep null rejection centralized in `Student`, and add intentional repository fetch behavior such as `@EntityGraph` or explicit queries for student reads that need the responsável loaded. Do not add join tables, relationship metadata, or many-to-many abstractions; D-05 explicitly removes that scope.</action>
  <acceptance_criteria>The database and entity model still use `tb_students.parent_id`; the `Student` aggregate clearly enforces one required responsável; repository reads intentionally load the current responsável for downstream plans.</acceptance_criteria>
  <verify>
    <automated>cd server/api-aprimorar &amp;&amp; ./mvnw -Dtest=StudentTest test</automated>
  </verify>
  <done>The backend has a hardened single-responsável foundation that later service and UI plans can build on without revisiting scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service/domain → persisted `parent_id` relation | registry integrity depends on aggregate rules matching the required responsável link |
| repository fetch plan → DTO mapping | downstream responses depend on the responsável being loaded intentionally, not accidentally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | I | `Student` aggregate | mitigate | Enforce the required responsável inside the entity so later service or UI paths cannot persist a student without a linked responsável. |
| T-02-02 | I | `StudentRepository` read paths | mitigate | Use intentional fetch behavior for `parent` on student reads so later DTO mapping and UI changes do not rely on lazy-loading side effects. |
</threat_model>

<verification>
- `cd server/api-aprimorar && ./mvnw -Dtest=StudentTest test`
- Review student repository read methods to confirm the linked responsável is loaded intentionally.
</verification>

<success_criteria>
- The storage model remains one student to one required responsável.
- No student can be persisted without a linked responsável.
- Student read paths are ready for contract and UI hardening without hidden lazy-loading risk.
</success_criteria>

<output>
After completion, create `.planning/phases/02-student-parent-registry-hardening/02-student-parent-registry-hardening-01-SUMMARY.md`
</output>
