---
phase: 02-student-parent-registry-hardening
plan: 04
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - client/src/features/parents/forms/parentFormSchema.ts
  - client/src/features/parents/hooks/parent-mutations.ts
  - client/src/features/parents/pages/ParentsPage.tsx
  - client/src/features/parents/pages/ParentCreatePage.tsx
  - client/src/features/parents/pages/ParentEditPage.tsx
  - client/src/features/parents/pages/ParentDetailPage.tsx
  - client/src/features/parents/components/ParentsTable.tsx
  - client/src/features/parents/components/ArchiveParentButton.tsx
  - client/src/features/parents/components/DeleteParentButton.tsx
autonomous: true
requirements:
  - PARN-01
  - PARN-02
  - PARN-03
  - PARN-04
must_haves:
  truths:
    - Secretary can still create, update, search, archive, and review responsável records with the existing core contact data intact.
    - Responsável pages use the Phase 2 terminology consistently.
    - Responsável detail/archive/delete UX reflects the new linked-student integrity rules instead of silently failing.
  artifacts:
    - path: client/src/features/parents/pages/ParentsPage.tsx
      provides: search/filter/archive list orchestration with consistent `Responsável` terminology.
      contains: Responsável
    - path: client/src/features/parents/pages/ParentDetailPage.tsx
      provides: relationship-aware responsible detail view with linked students.
      contains: useGetStudentsByParent
    - path: client/src/features/parents/hooks/parent-mutations.ts
      provides: mutation toasts/error handling for archive/delete integrity responses.
      contains: toast
  key_links:
    - from: client/src/features/parents/pages/ParentDetailPage.tsx
      to: client/src/kubb/hooks/student/useGetStudentsByParent.ts
      via: linked-student query on responsible detail
      pattern: useGetStudentsByParent
    - from: client/src/features/parents/components/ArchiveParentButton.tsx
      to: client/src/features/parents/hooks/parent-mutations.ts
      via: archive mutation error surfacing
      pattern: useParentMutations
---

<objective>
Harden the responsável-facing SPA pages so CRUD/search/archive flows remain trustworthy under the new relationship rules.

Purpose: complete Phase 2 from the responsible-party side without introducing finance-era concepts that the user explicitly deferred.
Output: consistent responsável terminology, dependable parent/responsável CRUD pages, and relationship-aware detail/archive/delete UX.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-student-parent-registry-hardening/02-CONTEXT.md
@.planning/phases/02-student-parent-registry-hardening/02-02-PLAN.md
@client/src/features/parents/forms/parentFormSchema.ts
@client/src/features/parents/pages/ParentsPage.tsx
@client/src/features/parents/pages/ParentCreatePage.tsx
@client/src/features/parents/pages/ParentEditPage.tsx
@client/src/features/parents/pages/ParentDetailPage.tsx
@client/src/features/parents/components/ParentsTable.tsx

<interfaces>
Current responsible CRUD boundary stays the existing contact record:

```ts
type ParentFormSchema = {
  name: string;
  email: string;
  contact: string;
  cpf: string;
};
```

Keep this core record stable so PARN-01/PARN-02 stay dependable, while reflecting the new link-aware read behavior from Plan 02-02. D-03 still applies: a responsável can exist without linked students.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize responsável terminology and preserve core CRUD/search flows</name>
  <files>client/src/features/parents/forms/parentFormSchema.ts, client/src/features/parents/pages/ParentsPage.tsx, client/src/features/parents/pages/ParentCreatePage.tsx, client/src/features/parents/pages/ParentEditPage.tsx, client/src/features/parents/components/ParentsTable.tsx</files>
  <read_first>client/src/features/parents/forms/parentFormSchema.ts, client/src/features/parents/pages/ParentsPage.tsx, client/src/features/parents/components/ParentsTable.tsx</read_first>
  <action>Keep the existing responsável core fields (`name`, `email`, `contact`, `cpf`) dependable for PARN-01/PARN-02, but update list/form copy so `Responsável` is the primary business term per D-10. Preserve search, filters, pagination, archive visibility, and current brownfield layout patterns. Do not add primary/financial/billing-owner fields here, because D-05 explicitly defers that concept; instead, keep the current contact record stable so finance can build on it later.</action>
  <acceptance_criteria>Responsável pages/forms consistently use the chosen terminology, continue to validate the existing contact fields, and keep list/search/archive controls unchanged.</acceptance_criteria>
  <verify>
    <automated>cd client &amp;&amp; npx eslint src/features/parents/forms/parentFormSchema.ts src/features/parents/pages/ParentsPage.tsx src/features/parents/pages/ParentCreatePage.tsx src/features/parents/pages/ParentEditPage.tsx src/features/parents/components/ParentsTable.tsx</automated>
  </verify>
  <done>The responsible-party CRUD/search surfaces remain stable and linguistically aligned with Phase 2 decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Reflect link-aware detail and archive/delete guardrails on responsável screens</name>
  <files>client/src/features/parents/pages/ParentDetailPage.tsx, client/src/features/parents/hooks/parent-mutations.ts, client/src/features/parents/components/ArchiveParentButton.tsx, client/src/features/parents/components/DeleteParentButton.tsx</files>
  <read_first>client/src/features/parents/pages/ParentDetailPage.tsx, client/src/features/parents/hooks/parent-mutations.ts, client/src/features/parents/components/ArchiveParentButton.tsx, client/src/features/parents/components/DeleteParentButton.tsx</read_first>
  <action>Update `ParentDetailPage` so linked students still load through `useGetStudentsByParent` against the new backend join logic and the page copy refers to `Responsável`. Use mutation error handling/toasts to surface the new server-side integrity rules when archive/delete would orphan an active student per D-09. Preserve D-03 by keeping standalone responsibles valid records, and keep the linked-student presentation concise rather than turning this screen into the main relationship editor; D-07 keeps link management anchored in the student flow.</action>
  <acceptance_criteria>Responsável detail still shows linked students, archive/delete errors are surfaced clearly in Portuguese, and the page does not require the user to leave the screen unsure why an integrity-protected action failed.</acceptance_criteria>
  <verify>
    <automated>cd client &amp;&amp; npx eslint src/features/parents/pages/ParentDetailPage.tsx src/features/parents/hooks/parent-mutations.ts src/features/parents/components/ArchiveParentButton.tsx src/features/parents/components/DeleteParentButton.tsx</automated>
  </verify>
  <done>The responsible-party screens faithfully reflect the new relationship model and lifecycle guardrails.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| secretary action → responsável archive/delete mutations | destructive UI actions rely on server integrity checks and clear feedback |
| generated linked-student query → responsável detail UI | relationship data returned by the API is shown to the user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | R | responsável archive/delete UX | mitigate | Surface Portuguese error feedback from guarded mutations so blocked actions are explicit and auditable to the secretary instead of appearing to do nothing. |
| T-02-09 | I | `ParentDetailPage` linked-student display | mitigate | Keep linked students driven by the generated query contract and current filters so the user sees the real preserved relationship state after the backend migration. |
</threat_model>

<verification>
- `cd client && npx eslint src/features/parents/forms/parentFormSchema.ts src/features/parents/hooks/parent-mutations.ts src/features/parents/pages/ParentsPage.tsx src/features/parents/pages/ParentCreatePage.tsx src/features/parents/pages/ParentEditPage.tsx src/features/parents/pages/ParentDetailPage.tsx src/features/parents/components/ParentsTable.tsx src/features/parents/components/ArchiveParentButton.tsx src/features/parents/components/DeleteParentButton.tsx`
</verification>

<success_criteria>
- Responsável CRUD/search/archive flows stay dependable after the relationship-model change.
- The SPA uses `Responsável` terminology consistently.
- Archive/delete guardrails are visible to the user and do not silently break linked-student integrity.
</success_criteria>

<output>
After completion, create `.planning/phases/02-student-parent-registry-hardening/02-student-parent-registry-hardening-04-SUMMARY.md`
</output>
