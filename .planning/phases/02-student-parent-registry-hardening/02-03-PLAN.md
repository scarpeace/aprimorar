---
phase: 02-student-parent-registry-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - client/src/features/students/forms/studentFormSchema.ts
  - client/src/features/students/components/StudentResponsiblesField.tsx
  - client/src/features/parents/components/ParentSelectDropdown.tsx
  - client/src/features/students/pages/StudentCreatePage.tsx
  - client/src/features/students/pages/StudentEditPage.tsx
  - client/src/features/students/pages/StudentDetailsPage.tsx
  - client/src/features/students/components/StudentsTable.tsx
  - client/src/features/students/hooks/student-mutations.ts
autonomous: true
requirements:
  - STUD-01
  - STUD-02
  - STUD-04
  - PARN-04
must_haves:
  truths:
    - Secretary can create and edit a student while selecting one or more linked responsáveis in the student flow.
    - Student detail and list screens show the preserved responsável links without a separate parent fetch.
    - Student search/filter/pagination/archive controls still behave after the contract change.
  artifacts:
    - path: client/src/features/students/components/StudentResponsiblesField.tsx
      provides: repeatable responsável link editor used by student create/edit pages.
      contains: relationshipType
    - path: client/src/features/students/forms/studentFormSchema.ts
      provides: form validation for one-or-more linked responsáveis.
      contains: responsibles
    - path: client/src/features/students/pages/StudentDetailsPage.tsx
      provides: compact multi-responsável read view driven by student query data.
      contains: responsibles
  key_links:
    - from: client/src/features/students/pages/StudentCreatePage.tsx
      to: client/src/features/students/components/StudentResponsiblesField.tsx
      via: react-hook-form control wiring
      pattern: StudentResponsiblesField
    - from: client/src/features/students/pages/StudentDetailsPage.tsx
      to: client/src/kubb/types/StudentResponseDTO.ts
      via: rendered responsibles array from generated contract
      pattern: responsibles
---

<objective>
Rewire the student SPA flows around the new multi-responsável API while keeping the brownfield list/search UX stable.

Purpose: deliver the user-facing part of Phase 2 where student registration and editing become the canonical place to manage linked responsáveis.
Output: updated student schema, multi-responsável form UI, and relationship-aware create/edit/detail/list screens.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-student-parent-registry-hardening/02-CONTEXT.md
@.planning/phases/02-student-parent-registry-hardening/02-02-PLAN.md
@client/src/features/students/forms/studentFormSchema.ts
@client/src/features/students/pages/StudentCreatePage.tsx
@client/src/features/students/pages/StudentEditPage.tsx
@client/src/features/students/pages/StudentDetailsPage.tsx
@client/src/features/students/components/StudentsTable.tsx
@client/src/features/parents/components/ParentSelectDropdown.tsx

<interfaces>
Current student form boundary:

```ts
export const studentFormSchema = studentRequestDTOSchema.extend({
  parentId: z.string().min(1).uuid()
});
```

Replace it with a form contract aligned to Plan 02-02:

```ts
type StudentResponsibleFormItem = {
  responsibleId: string;
  relationshipType?: string;
};

type StudentFormSchema = {
  ...existingStudentFields,
  responsibles: StudentResponsibleFormItem[];
};
```

Student detail should read `studentQuery.data.responsibles` directly instead of calling `useGetParentById` separately.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the single-responsável field with a repeatable link editor</name>
  <files>client/src/features/students/forms/studentFormSchema.ts, client/src/features/students/components/StudentResponsiblesField.tsx, client/src/features/parents/components/ParentSelectDropdown.tsx</files>
  <read_first>client/src/features/students/forms/studentFormSchema.ts, client/src/features/parents/components/ParentSelectDropdown.tsx</read_first>
  <action>Refactor the student form boundary from `parentId` to `responsibles[]`. Create `StudentResponsiblesField.tsx` as the repeatable row editor for the student flow: each row must let the secretary choose an existing responsável, optionally fill `relationshipType` per D-06, and add/remove rows. Validate at least one row per D-02, prevent duplicate responsável selection, and keep the quick "novo responsável" escape hatch because D-07 says link management lives inside student create/edit. You may adapt `ParentSelectDropdown` into a row-level selector helper, but keep user-facing labels in Portuguese and use `Responsável` terminology per D-10.</action>
  <acceptance_criteria>The form schema requires `responsibles.length >= 1`; the new field component supports add/remove row actions, optional relationship type, duplicate prevention, and inline validation without introducing primary/financial concepts.</acceptance_criteria>
  <verify>
    <automated>cd client &amp;&amp; npx eslint src/features/students/forms/studentFormSchema.ts src/features/students/components/StudentResponsiblesField.tsx src/features/parents/components/ParentSelectDropdown.tsx</automated>
  </verify>
  <done>The student form now models one-or-more linked responsáveis explicitly and is ready to be wired into pages.</done>
</task>

<task type="auto">
  <name>Task 2: Wire student create and edit pages to the new responsibles array</name>
  <files>client/src/features/students/pages/StudentCreatePage.tsx, client/src/features/students/pages/StudentEditPage.tsx, client/src/features/students/hooks/student-mutations.ts</files>
  <read_first>client/src/features/students/pages/StudentCreatePage.tsx, client/src/features/students/pages/StudentEditPage.tsx, client/src/features/students/hooks/student-mutations.ts</read_first>
  <action>Update both student pages to render `StudentResponsiblesField`, map existing query data into `responsibles[]`, and submit the generated contract shape unchanged. Keep the single registration flow per D-01; do not split this into a separate enrollment step. Editing must replace links atomically so the user cannot save a student with zero responsáveis per D-08. Preserve all existing student fields, loading/error states, and toast/navigation behavior while removing debug-only logging from the create page.</action>
  <acceptance_criteria>Student create/edit submit the new contract shape, prefill existing responsibles on edit, and block empty-link submissions before the request is sent.</acceptance_criteria>
  <verify>
    <automated>cd client &amp;&amp; npx eslint src/features/students/pages/StudentCreatePage.tsx src/features/students/pages/StudentEditPage.tsx src/features/students/hooks/student-mutations.ts</automated>
  </verify>
  <done>The student write flows use the server-backed multi-responsável contract end to end.</done>
</task>

<task type="auto">
  <name>Task 3: Update student details and list presentation to use compact responsável summaries</name>
  <files>client/src/features/students/pages/StudentDetailsPage.tsx, client/src/features/students/components/StudentsTable.tsx</files>
  <read_first>client/src/features/students/pages/StudentDetailsPage.tsx, client/src/features/students/components/StudentsTable.tsx</read_first>
  <action>Remove the separate `useGetParentById` dependency from `StudentDetailsPage` and render the linked responsáveis from the student payload itself. Per D-11, keep list/detail presentation compact: on detail, show a concise grouped section with names/contact/CPF and relationship type only when present; on table/list surfaces, show a compact count or compact names summary instead of expanding the full manager UI there. Do not change existing search/filter/pagination/archive controls from `StudentsPage`/`StudentsTable`; STUD-04 must stay intact.</action>
  <acceptance_criteria>Student detail no longer depends on a secondary parent fetch; the list/detail screens show preserved responsável links in a compact way; table pagination/search UX remains unchanged.</acceptance_criteria>
  <verify>
    <automated>cd client &amp;&amp; npx eslint src/features/students/pages/StudentDetailsPage.tsx src/features/students/components/StudentsTable.tsx</automated>
  </verify>
  <done>Student read surfaces reflect the new relationship model without cluttering the brownfield registry screens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| secretary input → student form state | untrusted browser input shapes the relationship payload before API submission |
| generated API response → student details/list UI | the SPA must render multi-responsável data safely and consistently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | T | `StudentResponsiblesField` | mitigate | Prevent duplicate responsável selections and enforce at least one row in the schema/UI so accidental client-side tampering does not create invalid submissions. |
| T-02-07 | I | `StudentDetailsPage` / `StudentsTable` | mitigate | Render responsibles from the typed generated contract and keep output compact, avoiding stale secondary fetches that could disclose inconsistent linked data. |
</threat_model>

<verification>
- `cd client && npx eslint src/features/students/forms/studentFormSchema.ts src/features/students/components/StudentResponsiblesField.tsx src/features/parents/components/ParentSelectDropdown.tsx src/features/students/pages/StudentCreatePage.tsx src/features/students/pages/StudentEditPage.tsx src/features/students/pages/StudentDetailsPage.tsx src/features/students/components/StudentsTable.tsx src/features/students/hooks/student-mutations.ts`
</verification>

<success_criteria>
- Student create/edit flows manage one or more responsáveis directly in the student flow.
- Student detail/list screens show preserved responsável links without a second parent query.
- Existing student search/filter/pagination/archive UX remains usable.
</success_criteria>

<output>
After completion, create `.planning/phases/02-student-parent-registry-hardening/02-student-parent-registry-hardening-03-SUMMARY.md`
</output>
