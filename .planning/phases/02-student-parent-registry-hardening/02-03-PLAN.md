---
phase: 02-student-parent-registry-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - client/src/features/students/forms/studentFormSchema.ts
  - client/src/features/parents/components/ParentSelectDropdown.tsx
  - client/src/features/students/pages/StudentCreatePage.tsx
  - client/src/features/students/pages/StudentEditPage.tsx
  - client/src/features/students/pages/StudentDetailsPage.tsx
  - client/src/features/students/components/StudentsTable.tsx
  - client/src/features/students/hooks/student-mutations.ts
autonomous: true
requirements:
  - STUD-01
  - STUD-02
  - STUD-04
  - PARN-04
must_haves:
  truths:
    - Secretary can create and edit a student while selecting exactly one linked responsável in the student flow.
    - Student detail and list screens show the current responsável clearly, preferably without redundant fetch churn.
    - Student search/filter/pagination/archive controls still behave after the contract change.
  artifacts:
    - path: client/src/features/students/forms/studentFormSchema.ts
      provides: form validation for one required responsável.
      contains: parentId
    - path: client/src/features/students/pages/StudentDetailsPage.tsx
      provides: compact responsible read view driven by the student query data.
      contains: Responsável
  key_links:
    - from: client/src/features/students/pages/StudentCreatePage.tsx
      to: client/src/features/parents/components/ParentSelectDropdown.tsx
      via: react-hook-form control wiring for the required responsável field
      pattern: ParentSelectDropdown
    - from: client/src/features/students/pages/StudentDetailsPage.tsx
      to: client/src/kubb/types/StudentResponseDTO.ts
      via: rendered responsible summary from generated contract
      pattern: responsible
---

<objective>
Refine the student SPA flows around the current single-responsável API while keeping the brownfield list/search UX stable.

Purpose: deliver the user-facing part of Phase 2 without splitting registration or introducing many-to-many UI that the user explicitly removed from scope.
Output: updated student schema, refined responsável selection UX, and relationship-aware create/edit/detail/list screens.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-student-parent-registry-hardening/02-CONTEXT.md
@.planning/phases/02-student-parent-registry-hardening/02-02-PLAN.md
@client/src/features/students/forms/studentFormSchema.ts
@client/src/features/students/pages/StudentCreatePage.tsx
@client/src/features/students/pages/StudentEditPage.tsx
@client/src/features/students/pages/StudentDetailsPage.tsx
@client/src/features/students/components/StudentsTable.tsx
@client/src/features/parents/components/ParentSelectDropdown.tsx

<interfaces>
Current student form boundary:

```ts
export const studentFormSchema = studentRequestDTOSchema.extend({
  parentId: z.string().min(1).uuid()
});
```

Keep the single `parentId` form contract aligned to Plan 02-02, but tighten validation, labels, and read rendering around `Responsável` terminology.

Student detail should prefer `studentQuery.data.responsible` directly if Plan 02-02 adds that summary, instead of depending on a separate `useGetParentById` fetch.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden the existing responsável selector and single-link validation</name>
  <files>client/src/features/students/forms/studentFormSchema.ts, client/src/features/parents/components/ParentSelectDropdown.tsx</files>
  <read_first>client/src/features/students/forms/studentFormSchema.ts, client/src/features/parents/components/ParentSelectDropdown.tsx</read_first>
  <action>Keep the student form boundary based on `parentId`, but harden it so the required responsável is unmistakable in the UI and validation. Improve `ParentSelectDropdown` only as needed to fit the brownfield forms better, keep the quick "novo responsável" escape hatch because D-07 still anchors linking in student create/edit, and use `Responsável` terminology per D-10 wherever practical.</action>
  <acceptance_criteria>The form schema clearly requires one responsável; the selector provides solid inline validation and maintains the existing single-selection flow without introducing multi-link controls.</acceptance_criteria>
  <verify>
    <automated>cd client &amp;&amp; npx eslint src/features/students/forms/studentFormSchema.ts src/features/parents/components/ParentSelectDropdown.tsx</automated>
  </verify>
  <done>The student form clearly models one required responsável and is ready for page-level refinements.</done>
</task>

<task type="auto">
  <name>Task 2: Wire student create and edit pages to the hardened single-responsável flow</name>
  <files>client/src/features/students/pages/StudentCreatePage.tsx, client/src/features/students/pages/StudentEditPage.tsx, client/src/features/students/hooks/student-mutations.ts</files>
  <read_first>client/src/features/students/pages/StudentCreatePage.tsx, client/src/features/students/pages/StudentEditPage.tsx, client/src/features/students/hooks/student-mutations.ts</read_first>
  <action>Update both student pages to keep using the generated single-responsável contract cleanly, remove debug-only logging from the create page, and strengthen the copy/layout around `Responsável`. Keep the single registration flow per D-01; do not split this into a separate enrollment step. Editing must still block saving without a responsável per D-08. Preserve all existing student fields, loading/error states, and toast/navigation behavior.</action>
  <acceptance_criteria>Student create/edit continue to submit the current contract shape, prefill the selected responsável on edit, and block missing-responsável submissions before the request is sent.</acceptance_criteria>
  <verify>
    <automated>cd client &amp;&amp; npx eslint src/features/students/pages/StudentCreatePage.tsx src/features/students/pages/StudentEditPage.tsx src/features/students/hooks/student-mutations.ts</automated>
  </verify>
  <done>The student write flows use the server-backed single-responsável contract cleanly end to end.</done>
</task>

<task type="auto">
  <name>Task 3: Update student details and list presentation to use compact responsável summaries</name>
  <files>client/src/features/students/pages/StudentDetailsPage.tsx, client/src/features/students/components/StudentsTable.tsx</files>
  <read_first>client/src/features/students/pages/StudentDetailsPage.tsx, client/src/features/students/components/StudentsTable.tsx</read_first>
  <action>Prefer rendering the current responsável from the student payload itself if Plan 02-02 exposes that summary; otherwise keep any fallback narrowly scoped. Per D-11, keep list/detail presentation compact: on detail, show a concise responsável section with name/contact/CPF, and on table/list surfaces show a compact summary instead of inventing a new management UI. Do not change existing search/filter/pagination/archive controls from `StudentsPage`/`StudentsTable`; STUD-04 must stay intact.</action>
  <acceptance_criteria>Student detail can show the current responsável cleanly with minimal query churn; the list/detail screens present the relationship compactly; table pagination/search UX remains unchanged.</acceptance_criteria>
  <verify>
    <automated>cd client &amp;&amp; npx eslint src/features/students/pages/StudentDetailsPage.tsx src/features/students/components/StudentsTable.tsx</automated>
  </verify>
  <done>Student read surfaces reflect the hardened current relationship model without cluttering the brownfield registry screens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| secretary input → student form state | untrusted browser input shapes the required responsável payload before API submission |
| generated API response → student details/list UI | the SPA must render current responsável data safely and consistently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | T | student form / `ParentSelectDropdown` | mitigate | Enforce the required responsável in the schema/UI so accidental client-side tampering does not create invalid submissions. |
| T-02-07 | I | `StudentDetailsPage` / `StudentsTable` | mitigate | Render the responsável from the typed generated contract when available and keep output compact, avoiding stale secondary fetches that could disclose inconsistent linked data. |
</threat_model>

<verification>
- `cd client && npx eslint src/features/students/forms/studentFormSchema.ts src/features/parents/components/ParentSelectDropdown.tsx src/features/students/pages/StudentCreatePage.tsx src/features/students/pages/StudentEditPage.tsx src/features/students/pages/StudentDetailsPage.tsx src/features/students/components/StudentsTable.tsx src/features/students/hooks/student-mutations.ts`
</verification>

<success_criteria>
- Student create/edit flows manage the required responsável directly in the student flow.
- Student detail/list screens show the current responsável cleanly, preferably without a second parent query.
- Existing student search/filter/pagination/archive UX remains usable.
</success_criteria>

<output>
After completion, create `.planning/phases/02-student-parent-registry-hardening/02-student-parent-registry-hardening-03-SUMMARY.md`
</output>
