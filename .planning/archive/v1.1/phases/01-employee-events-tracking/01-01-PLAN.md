---
phase: "01"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - server/api-aprimorar/src/main/java/com/aprimorar/api/domain/event/repository/EventSpecifications.java
  - server/api-aprimorar/src/main/java/com/aprimorar/api/domain/event/EventService.java
  - server/api-aprimorar/src/main/java/com/aprimorar/api/domain/event/EventController.java
  - server/api-aprimorar/src/test/java/com/aprimorar/api/domain/event/EventControllerTest.java
  - client/package.json
autonomous: true
requirements: [TAB-01, TAB-02]

must_haves:
  truths:
    - "API endpoint /v1/events/{id}/employee accepts studentName parameter"
    - "Filtering by studentName is performed using a Case-Insensitive LIKE query in the database"
    - "Frontend hooks reflect the new studentName parameter"
  artifacts:
    - path: "server/api-aprimorar/src/main/java/com/aprimorar/api/domain/event/repository/EventSpecifications.java"
      provides: "Specific Specification for student name search"
    - path: "server/api-aprimorar/src/test/java/com/aprimorar/api/domain/event/EventControllerTest.java"
      provides: "Verification of parameter mapping"
  key_links:
    - from: "EventController.java"
      to: "EventService.java"
      via: "studentName parameter passing"
---

<objective>
Enhance the backend API to support filtered event listing by student name and synchronize the frontend API client.

Purpose: Provide the necessary data layer for the searchable events table on the employee detail page.
Output: Updated Spring Boot Controller/Service/Specs and synchronized Kubb hooks.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-employee-events-tracking/01-CONTEXT.md
@.planning/phases/01-employee-events-tracking/01-RESEARCH.md
@.planning/phases/01-employee-events-tracking/01-PATTERNS.md
@server/api-aprimorar/src/main/java/com/aprimorar/api/domain/event/EventController.java
@server/api-aprimorar/src/main/java/com/aprimorar/api/domain/event/EventService.java
@server/api-aprimorar/src/main/java/com/aprimorar/api/domain/event/repository/EventSpecifications.java
</context>

<tasks>

<task type="auto">
  <name>Implement Student Name Filter Specification</name>
  <files>server/api-aprimorar/src/main/java/com/aprimorar/api/domain/event/repository/EventSpecifications.java</files>
  <read_first>
    - server/api-aprimorar/src/main/java/com/aprimorar/api/domain/event/repository/EventSpecifications.java
    - .planning/phases/01-employee-events-tracking/01-RESEARCH.md (Specification Example)
  </read_first>
  <action>
    Add the `withStudentNameIgnoreCase` method to `EventSpecifications` class. 
    It must join the "student" table and perform a case-insensitive LIKE check on the "name" field.

    Implementation:
    ```java
    public static Specification<Event> withStudentNameIgnoreCase(String term) {
        return (root, query, cb) -> {
            if (term == null || term.trim().isEmpty()) return null;
            String pattern = "%" + term.toLowerCase() + "%";
            return cb.like(cb.lower(root.join("student").get("name")), pattern);
        };
    }
    ```
  </action>
  <verify>
    <automated>grep "withStudentNameIgnoreCase" server/api-aprimorar/src/main/java/com/aprimorar/api/domain/event/repository/EventSpecifications.java</automated>
  </verify>
  <acceptance_criteria>
    - File contains `public static Specification<Event> withStudentNameIgnoreCase(String term)`
    - Implementation uses `cb.like(cb.lower(root.join("student").get("name")), pattern)`
  </acceptance_criteria>
  <done>Specification for student name filtering implemented.</done>
</task>

<task type="auto">
  <name>Update Event Service and Controller</name>
  <files>
    - server/api-aprimorar/src/main/java/com/aprimorar/api/domain/event/EventService.java
    - server/api-aprimorar/src/main/java/com/aprimorar/api/domain/event/EventController.java
  </files>
  <read_first>
    - server/api-aprimorar/src/main/java/com/aprimorar/api/domain/event/EventService.java
    - server/api-aprimorar/src/main/java/com/aprimorar/api/domain/event/EventController.java
  </read_first>
  <action>
    1. Update `EventService.getEventsByEmployeeId`:
       - Change signature to `public PageDTO<EventResponseDTO> getEventsByEmployeeId(Pageable pageable, UUID employeeId, String studentName)`
       - Use `Specification<Event> spec = Specification.where(EventSpecifications.withEmployeeId(employeeId)).and(EventSpecifications.withStudentNameIgnoreCase(studentName));`
       - Change `eventRepo.findAllByEmployeeId(employeeId, pageable)` to `eventRepo.findAll(spec, pageable)`.

    2. Update `EventController.getEventByEmployeeId`:
       - Add `@RequestParam(required = false) String studentName` to the method arguments.
       - Pass `studentName` to `eventService.getEventsByEmployeeId(pageable, id, studentName)`.
  </action>
  <verify>
    <automated>cd server/api-aprimorar && ./mvnw compile</automated>
  </verify>
  <acceptance_criteria>
    - `EventService.java` contains `getEventsByEmployeeId(Pageable pageable, UUID employeeId, String studentName)`
    - `EventController.java` contains `@RequestParam(required = false) String studentName` in `getEventByEmployeeId`
    - Code compiles without errors
  </acceptance_criteria>
  <done>Service and Controller updated to handle studentName parameter.</done>
</task>

<task type="auto">
  <name>Verify Backend with Tests</name>
  <files>server/api-aprimorar/src/test/java/com/aprimorar/api/domain/event/EventControllerTest.java</files>
  <read_first>
    - server/api-aprimorar/src/test/java/com/aprimorar/api/domain/event/EventControllerTest.java
    - .planning/phases/01-employee-events-tracking/01-VALIDATION.md
  </read_first>
  <action>
    Update `EventControllerTest.java` to verify that the `studentName` query parameter is correctly received by the controller and passed to the service.

    Tests to add:
    - `getEventsByEmployeeId_WithStudentName_ShouldCallServiceWithParam`: 
      Perform `GET /v1/events/{id}/employee?studentName=John` and verify `eventService.getEventsByEmployeeId` is called with "John".
    - `getEventsByEmployeeId_WithSort_ShouldHandlePageable`:
      Perform `GET /v1/events/{id}/employee?sort=startDate,desc` and verify the `Pageable` contains the correct sort.
  </action>
  <verify>
    <automated>cd server/api-aprimorar && ./mvnw test -Dtest=EventControllerTest</automated>
  </verify>
  <acceptance_criteria>
    - `EventControllerTest.java` has tests for `studentName` filtering.
    - All tests in `EventControllerTest` pass.
  </acceptance_criteria>
  <done>Backend functionality verified with automated tests.</done>
</task>

<task type="auto">
  <name>Synchronize Frontend Hooks</name>
  <files>client/src/kubb/index.ts</files>
  <read_first>
    - client/package.json
  </read_first>
  <action>
    Run the Kubb synchronization command in the `client` directory to update the frontend API client and React Query hooks.
    Command: `npm run sync`
  </action>
  <verify>
    <automated>grep "studentName" client/src/kubb/index.ts || grep "studentName" client/src/kubb/gen/hooks/useGetEventsByEmployeeId.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run sync` completes successfully.
    - The generated hook for `getEventsByEmployeeId` now includes `studentName` in its parameters.
  </acceptance_criteria>
  <done>Frontend hooks synchronized with backend API changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → API | Student search term is untrusted input |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | EventSpecifications.java | mitigate | Use JPA CriteriaBuilder (`cb.like`) instead of string concatenation to prevent SQL injection. |
| T-01-02 | Information Disclosure | EventController.java | mitigate | Ensure `@PreAuthorize` or existing authentication context is respected (already handled by Spring Security). |
</threat_model>

<verification>
Verify backend filtering and hook synchronization:
1. `mvn test -Dtest=EventControllerTest` passes.
2. `client/src/kubb/` contains updated hook definitions.
</verification>

<success_criteria>
- Backend API accepts `studentName` for employee events.
- Backend filtering uses case-insensitive partial match.
- Frontend hooks are updated and ready for use.
</success_criteria>

<output>
After completion, create `.planning/phases/01-employee-events-tracking/01-01-SUMMARY.md`
</output>
