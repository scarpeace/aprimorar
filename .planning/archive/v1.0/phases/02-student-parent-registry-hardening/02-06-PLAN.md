---
phase: 02-student-parent-registry-hardening
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/features/parents/pages/ParentDetailPage.tsx
  - client/src/features/parents/components/DeleteParentButton.tsx
  - client/src/features/parents/utils/getActiveLinkedStudentsCount.ts
  - client/src/features/parents/utils/getActiveLinkedStudentsCount.test.ts
autonomous: true
gap_closure: true
requirements:
  - PARN-03
  - PARN-04
must_haves:
  truths:
    - Responsável delete guidance only blocks when active students are still linked.
    - Responsável detail copy distinguishes active links from archived history.
    - Archived-only linked history is covered by an automated frontend-side regression test.
  artifacts:
    - path: client/src/features/parents/utils/getActiveLinkedStudentsCount.ts
      provides: active-link counting based on `archivedAt` in the paged student payload.
      contains: archivedAt
    - path: client/src/features/parents/components/DeleteParentButton.tsx
      provides: delete modal blocker semantics aligned with active linked students only.
      contains: isBlocker
    - path: client/src/features/parents/pages/ParentDetailPage.tsx
      provides: detail-page copy that explains active versus archived linked students.
      contains: aluno(s)
  key_links:
    - from: client/src/features/parents/pages/ParentDetailPage.tsx
      to: client/src/kubb/hooks/student/useGetStudentsByParent.ts
      via: summary query plus paginated linked-student table
      pattern: useGetStudentsByParent
    - from: client/src/features/parents/components/DeleteParentButton.tsx
      to: client/src/features/parents/utils/getActiveLinkedStudentsCount.ts
      via: delete blocker calculation
      pattern: getActiveLinkedStudentsCount
---

<objective>
Close the responsável detail/delete UX gap so the screen reflects the same active-link integrity rule enforced by the backend.

Purpose: stop the secretary from seeing an over-strict delete blocker when only archived student history remains linked, while preserving Phase 2's integrity rules from D-03, D-08, and D-09.
Output: active-only delete blocker semantics, clearer detail copy, and automated coverage for archived-only linked histories.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-student-parent-registry-hardening/02-CONTEXT.md
@.planning/phases/02-student-parent-registry-hardening/02-VERIFICATION.md
@.planning/phases/02-student-parent-registry-hardening/02-student-parent-registry-hardening-04-SUMMARY.md
@client/src/features/parents/pages/ParentDetailPage.tsx
@client/src/features/parents/components/DeleteParentButton.tsx
@client/src/components/ui/delete-confirmation-modal.tsx
@client/src/kubb/hooks/student/useGetStudentsByParent.ts
@client/src/kubb/types/PageDTOStudentResponseDTO.ts
@client/src/kubb/types/StudentResponseDTO.ts

<interfaces>
Generated linked-student payload already exposes the active/archive signal needed by the UI:

```ts
export type StudentResponseDTO = {
  id: string;
  name: string;
  parentId: string;
  archivedAt?: string | null;
};

export type PageDTOStudentResponseDTO = {
  totalElements: number;
  content: StudentResponseDTO[];
};
```

Current delete blocker is too broad:

```tsx
const { data: parentStudents } = useGetStudentsByParent(parentId);
const linkedStudentsCount = parentStudents?.totalElements ?? 0;
const hasLinkedStudents = linkedStudentsCount > 0;
```

Align the UI to the server rule from Plan 02-02/04: only active linked students block archive/delete; archived history should stay visible but non-blocking.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add automated coverage for active-versus-archived linked student counting</name>
  <files>client/src/features/parents/utils/getActiveLinkedStudentsCount.ts, client/src/features/parents/utils/getActiveLinkedStudentsCount.test.ts</files>
  <behavior>
    - Test 1: a page containing only archived linked students returns zero active links.
    - Test 2: mixed linked students returns only the count of records whose `archivedAt` is null/undefined.
    - Test 3: missing or empty page content returns zero safely.
  </behavior>
  <action>Create a tiny pure utility that derives active linked-student counts from the paged student payload instead of mixing this rule into the UI components. Write the regression test first and run it with `tsx` so the plan satisfies the verification gap without introducing a full frontend test harness. Keep the helper narrowly scoped to the archived-history distinction required by PARN-03/PARN-04; do not add unrelated formatting or API code here.</action>
  <verify>
    <automated>cd client &amp;&amp; npx tsx --test src/features/parents/utils/getActiveLinkedStudentsCount.test.ts</automated>
  </verify>
  <done>An automated test proves that archived-only linked history does not count as an active blocker.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire responsável detail and delete guidance around active links only</name>
  <files>client/src/features/parents/pages/ParentDetailPage.tsx, client/src/features/parents/components/DeleteParentButton.tsx, client/src/features/parents/utils/getActiveLinkedStudentsCount.ts</files>
  <action>Use `getActiveLinkedStudentsCount()` in the responsible detail/delete flow so the modal blocker and explanatory copy align with the backend's active-link guardrail from D-09. Keep the paginated linked-student table for history review, but add a dedicated summary query if needed (for example `useGetStudentsByParent(parentId, { page: 0, size: 100 })`) so the blocker is calculated from the real linked set rather than the currently visible page. Update `ParentDetailPage` copy to distinguish active vínculos from archived history, and update `DeleteParentButton` so archived-only histories inform the user without blocking deletion. Do not invent a new relationship-management surface; per D-07, linking still belongs in student create/edit flows.</action>
  <verify>
    <automated>cd client &amp;&amp; npx tsx --test src/features/parents/utils/getActiveLinkedStudentsCount.test.ts &amp;&amp; npx eslint src/features/parents/pages/ParentDetailPage.tsx src/features/parents/components/DeleteParentButton.tsx src/features/parents/utils/getActiveLinkedStudentsCount.ts src/features/parents/utils/getActiveLinkedStudentsCount.test.ts</automated>
  </verify>
  <done>The responsável detail/delete UX now mirrors the server rule: active students block destructive actions, archived history does not.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated linked-student query → responsável detail/delete UI | server relationship data is translated into destructive-action guidance |
| secretary delete intent → delete confirmation modal | the UI must not overstate or understate integrity blockers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | T | `DeleteParentButton.tsx` blocker logic | mitigate | Derive blockers from active linked students only and lock the rule with an automated helper test. |
| T-02-13 | I | `ParentDetailPage.tsx` relationship guidance | mitigate | Show separate active-versus-archived guidance so the secretary sees the real lifecycle rule before attempting archive/delete. |
</threat_model>

<verification>
- `cd client && npx tsx --test src/features/parents/utils/getActiveLinkedStudentsCount.test.ts`
- `cd client && npx eslint src/features/parents/pages/ParentDetailPage.tsx src/features/parents/components/DeleteParentButton.tsx src/features/parents/utils/getActiveLinkedStudentsCount.ts src/features/parents/utils/getActiveLinkedStudentsCount.test.ts`
</verification>

<success_criteria>
- Archived-only linked student history no longer blocks responsible deletion guidance.
- Parent detail copy explains active links separately from archived history.
- A lightweight automated test protects the active-link counting rule.
</success_criteria>

<output>
After completion, create `.planning/phases/02-student-parent-registry-hardening/02-student-parent-registry-hardening-06-SUMMARY.md`
</output>
