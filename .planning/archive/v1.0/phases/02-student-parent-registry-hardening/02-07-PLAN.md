---
phase: 02-student-parent-registry-hardening
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/ui/text-input.tsx
  - client/package.json
  - client/package-lock.json
autonomous: true
gap_closure: true
requirements:
  - STUD-01
  - STUD-02
  - STUD-04
  - PARN-01
  - PARN-02
  - PARN-03
  - PARN-04
must_haves:
  truths:
    - Shared form inputs render validation errors without breaking TypeScript compilation.
    - The SPA dependency set no longer pulls the broken `react-router` type declaration that stops `npm run build`.
    - Phase 2 registry flows are buildable again from the current codebase.
  artifacts:
    - path: client/src/components/ui/text-input.tsx
      provides: syntactically valid shared text input error rendering.
      contains: TriangleAlert
    - path: client/package.json
      provides: a React Router DOM version whose published type declarations are valid under the project's TypeScript setup.
      contains: react-router-dom
    - path: client/package-lock.json
      provides: refreshed dependency graph for a successful frontend build.
      contains: react-router
  key_links:
    - from: client/src/components/ui/text-input.tsx
      to: react-hook-form
      via: `useFormContext().formState.errors`
      pattern: errors
    - from: client/package.json
      to: client/node_modules/react-router/dist/development/index.d.mts
      via: installed router type package selected during `tsc -b`
      pattern: react-router-dom
---

<objective>
Remove the remaining frontend build blockers so the Phase 2 registry work is operationally shippable.

Purpose: Phase 2 cannot replace spreadsheets while `npm run build` fails, so this plan restores baseline SPA build health without changing product scope.
Output: fixed shared input syntax, upgraded router package declarations, and a passing frontend build.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-student-parent-registry-hardening/02-VERIFICATION.md
@.planning/phases/02-student-parent-registry-hardening/02-student-parent-registry-hardening-02-SUMMARY.md
@.planning/phases/02-student-parent-registry-hardening/02-student-parent-registry-hardening-04-SUMMARY.md
@client/src/components/ui/text-input.tsx
@client/package.json
@client/tsconfig.app.json

<interfaces>
Current shared input bug:

```tsx
{errors && (
  <p className="label text-error">
    <TriangleAlert className="w-3 h-3" />
    {errors.}
  </p>
)}
```

Current router build blocker from the installed package:

```ts
// client/node_modules/react-router/dist/development/index.d.mts
declare function (): NavigateFunction;
```

`react-router@7.14.1` publishes the corrected declaration:

```ts
declare function useNavigate(): NavigateFunction;
```

Fix the project through source-controlled files only. Do not patch `node_modules`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repair shared text input error rendering</name>
  <files>client/src/components/ui/text-input.tsx</files>
  <action>Replace the broken `{errors.}` expression with valid React Hook Form error rendering that reads the message for the current `identifier` safely and keeps the existing alert/icon layout. Preserve the current shared input API and Portuguese validation flow used across student and responsável forms; this is a shippability fix, not a redesign of the form component set.</action>
  <verify>
    <automated>cd client &amp;&amp; npx eslint src/components/ui/text-input.tsx</automated>
  </verify>
  <done>The shared text input compiles and still shows validation feedback for form fields.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade React Router types to a build-safe release and confirm the SPA build</name>
  <files>client/package.json, client/package-lock.json</files>
  <action>Upgrade `react-router-dom` to `7.14.1` and refresh `package-lock.json` so the resolved `react-router` package no longer ships the broken anonymous function declaration that currently fails `tsc -b`. Do not edit installed files under `node_modules`; fix the dependency source of truth only. After reinstall/lock refresh, rerun the full frontend build and confirm the registry SPA compiles cleanly with the existing Vite + TypeScript + bundler configuration.</action>
  <verify>
    <automated>cd client &amp;&amp; npm run build</automated>
  </verify>
  <done>`npm run build` passes, proving the Phase 2 SPA is operationally buildable again.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared form component → browser validation feedback | malformed code can hide validation errors and break registry form availability |
| package manifest → installed router declarations | dependency resolution controls whether TypeScript can compile the SPA |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-14 | D | `client/src/components/ui/text-input.tsx` | mitigate | Restore valid error rendering so the shared form component no longer crashes TypeScript compilation. |
| T-02-15 | D | `client/package.json` router dependency | mitigate | Pin `react-router-dom` to 7.14.1 and refresh the lockfile so `tsc -b` consumes the corrected published declaration instead of the broken 7.13.1 type file. |
</threat_model>

<verification>
- `cd client && npx eslint src/components/ui/text-input.tsx`
- `cd client && npm run build`
</verification>

<success_criteria>
- Shared text input code is syntactically valid and preserves form error feedback.
- React Router declarations no longer break TypeScript parsing.
- `npm run build` succeeds for the frontend.
</success_criteria>

<output>
After completion, create `.planning/phases/02-student-parent-registry-hardening/02-student-parent-registry-hardening-07-SUMMARY.md`
</output>
