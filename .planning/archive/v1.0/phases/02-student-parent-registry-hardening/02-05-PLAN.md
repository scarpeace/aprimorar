---
phase: 02-student-parent-registry-hardening
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - server/api-aprimorar/src/main/java/com/aprimorar/api/domain/student/repository/StudentSpecifications.java
  - server/api-aprimorar/src/main/java/com/aprimorar/api/domain/student/StudentService.java
  - server/api-aprimorar/src/test/java/com/aprimorar/api/domain/student/StudentServiceTest.java
  - server/api-aprimorar/src/test/java/com/aprimorar/api/domain/student/repository/StudentSpecificationsTest.java
autonomous: true
gap_closure: true
requirements:
  - STUD-04
must_haves:
  truths:
    - Secretary can search students by CPF from the existing student list.
    - The default student list hides archived records, and the archived toggle intentionally switches to archived results.
    - Student discovery rules stay covered by automated backend tests.
  artifacts:
    - path: server/api-aprimorar/src/main/java/com/aprimorar/api/domain/student/repository/StudentSpecifications.java
      provides: CPF-aware student search and archived/not-archived specification helpers.
      contains: searchContainsIgnoreCase
    - path: server/api-aprimorar/src/main/java/com/aprimorar/api/domain/student/StudentService.java
      provides: default list semantics that exclude archived students unless explicitly requested.
      contains: getStudents
    - path: server/api-aprimorar/src/test/java/com/aprimorar/api/domain/student/StudentServiceTest.java
      provides: regression coverage for archived toggle behavior.
      contains: getStudents
  key_links:
    - from: client/src/features/students/pages/StudentsPage.tsx
      to: server/api-aprimorar/src/main/java/com/aprimorar/api/domain/student/StudentService.java
      via: archived/search query params on /v1/students
      pattern: archived
    - from: server/api-aprimorar/src/main/java/com/aprimorar/api/domain/student/StudentService.java
      to: server/api-aprimorar/src/main/java/com/aprimorar/api/domain/student/repository/StudentSpecifications.java
      via: composed list specification
      pattern: StudentSpecifications\.
---

<objective>
Close the student discovery gap so Phase 2 search and archived visibility behave exactly as promised by the existing UI.

Purpose: restore trustworthy student lookup for daily registry work without changing the single-responsável model locked by D-04 and D-06.
Output: CPF-aware student search, correct default archived filtering, and backend regression tests for both behaviors.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-student-parent-registry-hardening/02-CONTEXT.md
@.planning/phases/02-student-parent-registry-hardening/02-VERIFICATION.md
@.planning/phases/02-student-parent-registry-hardening/02-student-parent-registry-hardening-02-SUMMARY.md
@client/src/features/students/pages/StudentsPage.tsx
@server/api-aprimorar/src/main/java/com/aprimorar/api/domain/student/repository/StudentSpecifications.java
@server/api-aprimorar/src/main/java/com/aprimorar/api/domain/student/StudentService.java
@server/api-aprimorar/src/test/java/com/aprimorar/api/domain/student/StudentServiceTest.java

<interfaces>
Current list contract exposed by the UI:

```tsx
const studentsQuery = useGetStudents({
  page: currentPage,
  search: debouncedSearchTerm,
  archived: showArchived,
});
// placeholder: "Buscar aluno por nome, email ou CPF"
```

Current backend gap:

```java
public static Specification<Student> searchContainsIgnoreCase(String term) {
    return cb.or(
        cb.like(cb.lower(root.get("name")), likeTerm),
        cb.like(cb.lower(root.get("email")), likeTerm),
        cb.like(cb.lower(root.get("school")), likeTerm)
    );
}

public PageDTO<StudentResponseDTO> getStudents(Pageable pageable, String search, Boolean archived) {
    Specification<Student> spec = StudentSpecifications.isNotGhost();
    if (Boolean.TRUE.equals(archived)) {
        spec = spec.and(StudentSpecifications.archived());
    }
    ...
}
```

Preserve D-04/D-06: this plan only fixes discovery semantics; it must not redesign student-responsável linkage.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock student discovery semantics with failing tests</name>
  <files>server/api-aprimorar/src/test/java/com/aprimorar/api/domain/student/StudentServiceTest.java, server/api-aprimorar/src/test/java/com/aprimorar/api/domain/student/repository/StudentSpecificationsTest.java</files>
  <behavior>
    - Test 1: a CPF fragment matches the student search specification used by the list endpoint.
    - Test 2: `getStudents(..., archived=false)` and `getStudents(..., null)` exclude archived records by composing `notArchived()`.
    - Test 3: `getStudents(..., archived=true)` still returns archived records intentionally.
  </behavior>
  <action>Before changing production code, add focused regression coverage for the two missing items called out in `02-VERIFICATION.md`. Extend `StudentServiceTest` so the service-level archived toggle semantics are explicit, and add `StudentSpecificationsTest` if needed to prove CPF participates in `searchContainsIgnoreCase()`. Keep Portuguese naming/messages aligned with the current student domain, and do not add unrelated list filters or any relationship-model changes. This implements the Phase 2 search promise per D-11 while preserving the single-responsável rules from D-04/D-06.</action>
  <verify>
    <automated>cd server/api-aprimorar &amp;&amp; ./mvnw -Dtest=StudentServiceTest,StudentSpecificationsTest test</automated>
  </verify>
  <done>The test suite fails before the production fix and documents CPF search plus archived default behavior clearly enough that later regressions are caught automatically.</done>
</task>

<task type="auto">
  <name>Task 2: Implement CPF-aware search and default non-archived list behavior</name>
  <files>server/api-aprimorar/src/main/java/com/aprimorar/api/domain/student/repository/StudentSpecifications.java, server/api-aprimorar/src/main/java/com/aprimorar/api/domain/student/StudentService.java</files>
  <action>Update `StudentSpecifications.searchContainsIgnoreCase()` so CPF is part of the existing OR search alongside name/email/school, matching the UI promise already present in `StudentsPage.tsx`. In `StudentService.getStudents()`, treat `archived=false` and `archived=null` as the default active-list path by composing `StudentSpecifications.notArchived()`, while keeping `archived=true` mapped to `StudentSpecifications.archived()`. Preserve the ghost-student exclusion, current pagination contract, and existing Phase 2 write/link behavior; this task only fixes the failed STUD-04 discovery truth.</action>
  <verify>
    <automated>cd server/api-aprimorar &amp;&amp; ./mvnw -Dtest=StudentServiceTest,StudentSpecificationsTest test</automated>
  </verify>
  <done>Student list queries now honor CPF search and archived toggle semantics exactly as the secretary-facing screen describes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser search/toggle params → `/v1/students` | untrusted query params influence which student records are listed |
| `StudentService` → `StudentSpecifications` | list safety depends on composing the right filters before repository execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | I | student list endpoint | mitigate | Compose `notArchived()` by default so archived records are not disclosed in the standard active list unless the secretary intentionally asks for them. |
| T-02-11 | T | `StudentSpecifications.searchContainsIgnoreCase` | mitigate | Cover CPF search in automated tests and extend the specification directly instead of relying on UI placeholder text. |
</threat_model>

<verification>
- `cd server/api-aprimorar && ./mvnw -Dtest=StudentServiceTest,StudentSpecificationsTest test`
</verification>

<success_criteria>
- Student search matches CPF as advertised on the existing list screen.
- Default student lists exclude archived records.
- Archived-only view still works when the secretary enables the toggle.
</success_criteria>

<output>
After completion, create `.planning/phases/02-student-parent-registry-hardening/02-student-parent-registry-hardening-05-SUMMARY.md`
</output>
